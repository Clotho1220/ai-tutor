# AI Tutor：穩定性修改交接清單

日期：2026-09-14
基準：v3.49 的 Markdown 與部分程式靜態審查；未進行本次實機驗證。
對象：6–8 歲兒童，沿用現有 Gogo English 教材。

## 給 Claude 的任務

請依本清單改善一般課計畫模式的 AI 穩定性，先完成 P0，再完成 P1。使用者的核心問題是：不知道多次修改後實際送給模型哪些指令，以及 AI 反覆、跳題、沉默、前後文銜接不一致。

先讀 CLAUDE.md、DEVLOG.md 的現況與最新待辦，再核對目前程式。以下行號是審查時的位置，修改後可能移動，請以函式名稱定位。若專案已有更新，保留已完成的修正，避免倒退。

本輪重點是提示詞、狀態、回合與恢復機制。保留教材、週進度、題型、重試上限、字母真人錄音與播放模式、點選由程式判分、現有手機音訊路由。成人與時事模式獨立保留，不讓其規則混入兒童計畫課。不要順帶換模型、加第二條轉錄服務、加即時生圖、改教學進度或引入錄音上傳。

使用者希望節省 GPT 額度與測試成本：開發驗證以離線測試和事件重播為主。未獲具體測試預算前，不自動批次呼叫付費語音 API。尚未做過的真人語音驗證必須標示未驗，不能用 mock 通過宣稱已完全修好。

## 已確認的依據與尚待驗證的原因

1. app.js / buildSystemInstruction（約 1065–1164）：planContract 後仍串接 TURN_CONTRACT 及舊教學規則。現存要求包含「回饋後再問一題」「同句型最多兩次」「提到名詞呼叫 show_image」，與計畫模式的等待、指定代換、前端控圖衝突。
2. app.js / GPT onTurnComplete（約 1720–1735）：仍可能用 updateInstructions 加回句型家族換題限制。enforcePracticeCap 雖有 planDriving() 防護，這條更新路徑也要核對。
3. app.js / report_item_result schema、recordItemResult（約 2482、3137）：回報主要用 target 文字與時序推測匹配，schema 沒有明確 itemId/attemptId。無效 outcome 會落到 incorrect；itemResults.push 在部分合法性檢查之前。
4. app.js / queuePlanDirective、advancePlan、planFallbackAfterTurn：已有排隊、補問、兜底，不應另加一套平行機制。
5. openai-realtime.js：已有 responseInProgress、取消等待、responseCreatePending，且區分生成完成與音訊播放完成。不要把 response.done 當成孩子已聽完；也不要盲目恢復曾造成問題的 truncate 作法。
6. lesson-plan.js / createRunner：unknown 兜底用 done 前進。這可以維持課程流動，但不是學生已答對的證據。
7. DEVLOG.md §8：v3.49 沉默修正、點選操作仍待實機驗證。

上述是靜態事實；「模型反覆都是記憶不足」「一定是網路延遲」「新模型就能解決」均未被證實。先做可追查的記錄，才能分辨真正原因。

## P0-1：讓實際提示詞可看、可追溯

### 要改什麼

- 加入家長／開發設定中的「查看本堂 AI 指令」「匯出診斷」能力；學生畫面不顯示工程內容。
- 分開呈現：實際送出的完整 system instructions、工具 schema、當前題目指令、之後每次 instructions 更新與發送原因。
- 快照必須從真正的發送路徑取得，不能事後重新呼叫組裝函式冒充當時內容。
- 每份快照記 promptVersion、appVersion、provider、mode、sessionId、時間、內容雜湊／識別碼。
- 同一份大指令以識別碼引用，避免每個事件都複製全文；保留有界限的本機紀錄與明確匯出，不增加雲端上傳。
- 僅收集指令與必要事件欄位，不記 API Key、短效憑證、認證 header 或原始錄音。既有學生逐字稿不擴大保存範圍。

### 驗收

- 連線後的指令快照，與傳輸層真正送出的內容相同。
- 任一 updateInstructions 都能查到更新前後與原因；重連的快照也有紀錄。
- 能回答：「這堂課使用哪版指令？中途被誰改了？」

## P0-2：拆開模式，清除衝突指令與舊行為注入

### 要改什麼

- 建立單一提示詞組裝入口，按模式選擇規則，不把兒童計畫課、成人聊天、時事與舊流程規則串成一大段。
- 兒童計畫課只保留：角色與簡短語言風格、一次處理當前動作、如何等待／回報、澄清與求助處理。
- 題目數量、代換數量、重試次數、換題、下課由程式控制，不再放一套模型自行判定的競爭規則。
- 從兒童計畫課排除：每回合必須追加問題、同句型全堂兩次上限、自行找新活動、提到名詞就呼叫圖片工具、每次答對都加替代表達。
- 排查所有 updateInstructions、DIRECTOR NOTE、重複偵測與課程階段更新入口。停用計畫模式不應收到的 GPT family update，而不只是改 TURN_CONTRACT 的文字。
- 點選階段不要求模型判分或回報學生正誤；口說階段只處理當前作答。按模式／階段控制可用工具；兩家供應商的 schema 仍來自同一份定義。
- 開場、結尾另有明確完成事件，不用虛構學生作答結果來推進。

### 建議的規則骨架

「你是協助 6–8 歲孩子練習本課教材的老師。當前動作由課程系統提供。出題時提出這一道題並等待；收到本題作答時給簡短回饋並回報這一次結果；收到澄清請求時解釋剛才的題目；完成後等待系統安排。圖片、提示、下一題及結束由系統決定。」

這是責任邊界示意，請配合狀態與工具合約形成精簡指令，不直接把本文件整段塞給模型。

### 驗收

- 抽取實際兒童計畫課指令，確認不含上述舊規則；成人模式仍保有自然追問能力。
- 一個句型計畫安排三個合法項目時，不會被舊家族上限提前趕去換題。
- 點選題不會同時收到「必須回報」與「不得回報」要求。

## P0-3：使用明確題目／作答識別碼，防止重複與過期回報

### 要改什麼

- 程式建立 sessionId、itemId、attemptId、directiveId，以及需要的連線世代 connectionEpoch。對同一動作的傳輸重試保留識別碼；新作答才建立新的 attemptId。
- 把必要識別碼交給模型回報，傳輸層保留 provider responseId、toolCallId 等來源關聯。模型提供的識別碼要驗證，不能直接相信。
- 同一個有效作答只能結算一次；同一回報重送、不同工具呼叫重報、上一題遲到、重連前遲到事件，均不能再推進。
- 檢查回報是否属于目前允許評估的口說作答。尚未出題／尚無學生作答時，不能用模型自行回報完成練習題。
- target 文字保留作為內容核對，不能再是唯一身分依據。缺識別碼／錯識別碼先記 protocol_error，不以文字模糊匹配偷偷接受。
- 無效 outcome 不預設 incorrect；協定失敗與學生答錯分開。
- 原始回報可留在診斷；只有通過合法性驗證的結果進入有效學習紀錄。點選題被忽略的模型回報不能污染答對率。
- 修正延遲 callback：例如 900ms 後的點選推進，需要驗證當時 itemId/attemptId，避免換題或結束後還改動新狀態。

### 驗收

相同回報來兩次、同一單字出現在兩道題、上一題回報遲到、點選連按、舊連線事件遲到、無效結果值：全部最多結算目前合法作答一次，不能跳題或算學生答錯。

## P1-1：建立最小上下文快照，讓模型知道「現在做到哪」

### 要改什麼

- 當前題目與下一個動作由程式狀態決定，不靠模型從整堂聊天記憶猜測。
- 新增最小狀態快照，例如：sessionId、itemId、attemptId、phase、inputMode、可見圖片／文字描述、提示層級、上一次有效題目、這次作答是否已處理、目前需要模型做的動作。
- 學生作答文字若尚無 final transcript，標示未取得／暫定，不憑空補寫；逐字稿與原始音訊評估不可混為一談。
- 快照按狀態變更產生，在安全的回合邊界更新；不要因送一份快照又觸發一次搶話回應。依供應商實際 API 能力實作，涉及新事件參數先核對官方文件。
- 歷史只帶必要的近期資訊；沒有證據顯示需要時，不加入全歷史摘要呼叫、向量資料庫或每題重連。
- 分開內部完整評分狀態與模型的本輪指令。不可把尚未揭露的答案、未來題目整包放進出題指令。評估階段需要的標準答案只供該階段使用，維持既有提示揭露規則。
- 重連先確認是原 session 續接還是新 session；按照目前 phase 恢復。學生已答但結果未知時，不冒充已答對，也不重播整堂開場。

### 驗收

中途短暫離題、澄清「什麼意思」、同題給提示、作答後斷線：回來都能接在正確階段，不把中文求助當新翻譯題，不重教已完成項目。

## P1-2：統一回合控制，分清生成、播放、輸入與結算

### 要改什麼

- 整理成單一課程狀態控制入口；可用 PRESENTING、WAITING_SPEECH、WAITING_TAP、EVALUATING、FEEDBACK、RECOVERING、CLOSING、ENDED 等狀態，名稱依現有架構調整。
- 傳輸層另外保留 responseActive、audioPlaying、studentTalking 等事實。生成結束不代表聲音播完；buffer cleared／cancelled 不代表內容完整播放。
- 保留既有 response.create 防撞、工具結果先回送、音訊排隊與手機路由，整合到單一排程，不另建互相競爭的推進途徑。
- next item、升提示、工具結果 continuation、沉默恢復、重連重送都走同一排程，並驗證當前識別碼。
- 工具呼叫回合也要正確結算生成狀態，避免工具分支提前 return 導致缺少必要完成通知；只有工具的回合是否正常由 phase 決定。
- 正常換題需等必要回饋播放完成與學生未說話。明確的使用者中斷則走取消路徑，清掉失效佇列。
- 如需修正模型對「孩子实际聽到哪裡」的認知，先驗證目前供應商播放／取消事件行為，再處理。不要直接恢復歷史上失敗的 truncate 操作。

### 驗收

AI 生成完但回饋還在播時，下一題不切斷聲音；一次工具回報不產生雙重 response；孩子說話期間不送搶話指令；清除音訊不會觸發正常下課。

## P1-3：合併恢復機制，停止「越修越多輪」

### 要改什麼

- 整合 silent turn、漏回報補問、off-script 重送與重連重試，共用可設定、有限的恢復預算；避免每條各重試一次形成連鎖迴圈。
- 出題階段應有可播放的題目；等待孩子回答／點選時的沉默正常，不因安靜自動催問。
- 不單靠逐字稿為空認定沒有聲音；結合音訊事件、播放狀態與預期 phase。只有工具的評估回合也不能一律視為壞掉。
- 純系統恢復不增加孩子的錯題次數、不升提示、不改成已掌握。
- 達到恢復上限後退出自動重送，進入明確的可恢復畫面。若必須跳過，標為 unverified／system_error，保留稍後補練；不要無聲跳過。
- unknown／done 的相容資料要保留「已走過但未確認」語意，報告不得當成 correct。

### 驗收

空白出題、漏回報、回報錯題、斷線連續發生時，重試次數仍有總上限；不反覆催孩子唸同一句，也不把系统失敗算成孩子失敗。

## P1-4：清楚顯示現在要「聽、說、點、等」

### 要改什麼

- 兒童畫面只顯示当前主要動作，例如「聽 Gogo 說」「換你說」「點一個答案」「稍等一下」。
- 點選階段若設計不處理語音，就不要同時要求孩子先說完才算成功。把必要口說安排為可辨識的下一階段；跟讀若僅供練習，不宣稱已驗證發音。
- 非作答的求助需要獨立入口或事件，不能算成作答錯誤。
- 技術恢復時給簡短、中性的提示及重試／結束操作，避免一動不動讓孩子一直重說。

### 驗收

不看家長說明，畫面也能看出当前要做什麼；說了但系統在等點擊時，有明確提示；UI 狀態與真實可接收的輸入一致。

## 測試與交付要求

### 先用零 API 成本測試

建立可注入事件的模擬 adapter／重播測試，沿用現有測試框架即可。使用真實 units.json 抽樣與合成學生事件，不需要上傳錄音。

至少覆蓋：

1. 正常口說答對、答錯後提示、到達重試上限。
2. 同一 toolCall 重送、不同 toolCall 重報同一 attempt、上一題回報遲到。
3. 同一 target 出現在不同 item，不可靠字串混淆。
4. 點選連按、900ms callback 遲到、點選題收到模型回報。
5. AI 已生成完但音訊未播完；audio clear、cancelled 與正常 stopped 的差別。
6. 工具回合完成、漏回報、空逐字稿但有音訊、真正空白出題。
7. 學生問「什麼意思」、操作求助、離題後回到本題。
8. 等待點選時斷線、作答後斷線、舊 connectionEpoch 事件遲到。
9. 收尾音訊未播完不斷線；系統恢復不增加學習錯誤數。
10. 順序打亂的合法／過期事件不造成雙推進；所有恢復合計不超過預算。

### 實機驗證

離線測試通過後，準備一個短測試課程，讓使用者用原本正在用的模型測：口說答對、答錯一次、求助、點選、斷線恢復。若沒有實機條件，交付操作步驟並標示待驗，停止付費 API 測試。

報告需分列：離線通過項目、實機已驗項目、尚未驗證項目。模型品質不能由離線測試保證。

### 診斷至少要回答

- 本堂課實際用哪份 prompt，何時被更新？
- 卡住的是生成、音訊播放、等待輸入、評估還是工具回報？
- 哪個 item／attempt 收到了幾次指令與結果？
- 重複來自模型真的重說、同一事件被處理兩次，還是程式重送？
- 系統共恢復幾次，有沒有把失敗誤算為答錯／完成？

### 修改順序與交付

1. P0-1 提示詞觀測 → P0-2 清理規則 → P0-3 識別碼與去重。
2. P1-1 上下文 → P1-2 回合控制 → P1-3 恢復 → P1-4 畫面動作。
3. 每一階段附對應測試，出現失敗先修，不靠新增更多全域提示詞繞過。
4. 更新現行規格、DEVLOG、HANDOFF、PROJECT-STATUS。修正五天／週曆、代換次數、舊家族限制及待驗項目的矛盾。歷史記錄標明適用版本，現況只保留一份權威描述。
5. 更新版本、快取與必要產生物，遵循專案既有發布流程；清楚記錄實際執行與未執行的檢查。
6. 最終回覆列明改了哪些責任邊界、刪掉哪些衝突規則、測試結果與剩餘風險。不要只回答「提示詞已優化」。
