# AI Tutor Studio 開發紀錄（DEVLOG）

最後更新：2026-09-16　目前版本：**v3.56**　正式站：<https://clotho1220.github.io/ai-tutor/>

這份是**整個專案的敘事紀錄**：為什麼這樣設計、遇過哪些問題、每個問題的根因與教訓、
現在做到哪、接下來要做什麼。**每次進版都要更新**（規則在 [`CLAUDE.md`](CLAUDE.md)）。

---

## 0. 文件地圖（新對話先看這裡）

| 文件 | 內容 | 什麼時候看 |
|---|---|---|
| [`CLAUDE.md`](CLAUDE.md) | 工作守則、進版檢查清單、環境陷阱 | 每次開新對話自動載入 |
| **`DEVLOG.md`（本檔）** | 設計理念、架構、問題全紀錄、進度、下一步 | 接手時通讀 §1–4、§8 |
| [`HANDOFF.md`](HANDOFF.md) | 使用者定義的教學規格、已做/未做對照表、已知陷阱、常用指令 | 要改教學流程前 |
| [`PROJECT-STATUS.md`](PROJECT-STATUS.md) | 逐版本變更（v3.10 → 現在），每版一節 | 查某個行為是哪一版、為什麼改的 |
| [`AI-STABILITY-HANDOFF.md`](AI-STABILITY-HANDOFF.md) | 穩定性修改規格（P0-1～P1-4），v3.50 據此實作 | 改提示詞、回報、恢復機制前 |

---

## 1. 專案是什麼

**給 6–8 歲小孩的中英雙語 AI 語音家教**（網頁 PWA，手機上用），也有一個成人英文對話模式。

| 學員 | 設定 |
|---|---|
| Rex | 約 70% 英文 |
| Jessie、Sandy（使用者的妹妹們） | 中英各半 |
| Clotho（使用者本人） | 成人對話模式 |

- **教材**：培生 *New Gogo Loves English* 1–3 冊（27 個正課單元 + 9 個 Review），外加自建的「字母 ABC」
- **語音模型**：Gemini Live 3.1（`gemini-3.1-flash-live-preview`）、GPT Realtime 2.1 mini（預設省錢）、GPT Realtime
- **部署**：純前端，GitHub Pages（`git push origin main` 即上線，約 20–40 秒生效）
- **後端**：Google Apps Script + Google 試算表（跨手機同步學習紀錄；API Key 放在指令碼屬性，手機只拿短效憑證）
- **素材來源**：隔壁專案 `../Gogo English/`（教材資料 JSON、766 張插畫母版、字母卡）

---

## 2. 設計理念（每一條都是踩過坑之後定下來的）

### 2.1 程式決定「做什麼」，模型只負責「演出來」
這是整個專案的主軸（v3.14–v3.19 逐步建立）。早期讓模型自己帶整堂課，結果它會自己跳題、
自己決定下課、同一句練八次。現在：

- 課前 `lesson-plan.js` 把今天每一項算成清單（**計畫模式，預設開啟**）
- `lesson-flow.js`（v3.50）持有 phase 與識別碼，一次只送**一個項目**的導演指令，開頭帶 `[STATE …]` 快照
- 模型練完呼叫 `report_item_result` 並帶 `attemptId`，程式驗證後才推進
- 模型漏回報、亂跑、沉默，都有程式端的兜底（補問、重送、unverified、跳過），**共用一個有上限的恢復預算**

### 2.2 能讓程式判斷的，就不要讓模型判斷
- **提示階梯＝嘗試次數**（v3.19）：同一個計數器決定「練了幾次」與「提示給到第幾層」，走完最後一階一定往下，結構上不會卡死
- **點選作答**（v3.40）：第 2–5 天的單字與對答改成畫面上點，對錯由程式判定。模型聽不準小孩拼字、語音轉文字會把發音「修正」掉——這兩個限制就這樣繞過去
- **字母單元完全不用模型**（v3.42）：本來就不判對錯，模型只會多話和延遲，直接拿掉

### 2.3 畫面由前端控制，不讓模型呼叫工具決定
圖、英文、中文要不要顯示，由提示階梯的 `reveal` 決定（`revealFor()` → `applyPlanReveal()`）。
模型只要一開口唸出還沒揭露的字，整個階梯就毀了——所以**該藏的資訊根本不放進指令**（v3.28）。

### 2.4 提示詞要正向、而且規則之間不能打架
- 否定句會被照著講（「不要說我們還沒下課」→ 模型就說了）
- **兩條規則打架，模型會選最省事的解法**：v3.42「不准以問句結尾」＋每題「問出來等他答」→ GPT 選擇整輪閉嘴（v3.49 的大 bug）
- 重複的固定尾巴會誘發跳針（v3.25）：通用規則只在系統提示講一次

### 2.5 只練教材內容；小孩的節奏優先
- 延伸單字已移除（v3.19）
- 五天課程對齊紙本練習卷：第 1 天中翻英 → 2 英翻中 → 3 三選一 → 4 填缺漏字母 → 5 拼字（v3.32）
- 週曆制換單元：每週日換下一個；一週內依「上完的堂數」推進（v3.31、v3.33）
- 做完所有項目才下課，時間只是護欄

### 2.6 驗證要看真實資料，不是看「看起來有在動」
這是這個專案最貴的教訓（見 §6.4）。卡片有在換 ≠ 聲音有在播；測試全過 ≠ 線上資料正確；
檔案長度正常 ≠ 錄音唸對了。**診斷檔、逐字稿、實際解碼、真的轉回文字**才算數。

---

## 3. 架構

### 3.1 兩種上課模式

```
                    ┌─ 一般課（Book 1–3）──────────────────────────────────────┐
開始連線 ─┬────────▶│ lesson-plan.build() → 項目清單                           │
          │         │   → 逐項送 DIRECTOR NOTE → Gemini / GPT 演出             │
          │         │   → report_item_result / 點選 → advancePlan() → 下一項   │
          │         └──────────────────────────────────────────────────────────┘
          │         ┌─ 字母 ABC（播放模式，不連線）────────────────────────────┐
          └────────▶│ ▶ 開始 → letter-player.js 逐張：卡片 → 教材真人音片段     │
                    │   → 留 1.5 秒給孩子唸 → 下一張（整輪 52 張，⏸ 可暫停）    │
                    └──────────────────────────────────────────────────────────┘
```

### 3.2 模組

| 檔案 | 職責 |
|---|---|
| `app.js` | 主程式：模型連線、指令排隊（單一出口）、執行流程控制器回傳的動作、播放模式入口 |
| `lesson-plan.js` | 純函式：組今日項目、提示階梯、`fillSlot` 代換、對話漫畫挑圖、點選題選項、`revealFor`、執行器 |
| `lesson-flow.js` | v3.50：課程流程控制器——識別碼、phase、回報驗證與去重、點選 token、恢復預算、重連、求助；純邏輯可重播測試 |
| `prompt-builder.js` | v3.50：提示詞唯一組裝入口，按模式（child-plan／child-flow／adult／news）各自組、決定可用工具 |
| `prompt-ledger.js` | v3.50：提示詞帳本，記錄實際送出的每份指令（hash 去重），進診斷檔與設定頁 |
| `letter-player.js` | 字母播放器：教材音軌片段（WebAudio）→ TTS 三段備援 → 瀏覽器語音備援 |
| `student-view.js` | 學生畫面：卡片、泡泡壓字、點選按鈕、▶ 開始、換你唸 |
| `openai-realtime.js` | GPT WebRTC、工具呼叫、防撞（response.create 只開一發） |
| `live-session.js` | Gemini 連線、重連、session resumption |
| `lesson-ending.js` | 下課／提早道別偵測 |
| `course-progression.js` | 單元推進（字母那本書 `standalone`，不接主線） |
| `session-diagnostics.js` | 課堂事件 → 可匯出的診斷 JSON（保留最近 20 堂） |
| `sync.gs` | Apps Script 後端：同步、憑證 |

### 3.3 資料流（產生物不要手改）

```
../Gogo English/教材資料/gogo{1,2,3}.json ─┐
units-overlay.json（人工整理句型/槽位/複數）─┼─ build-units-from-gogo.py ─▶ units.json（上課讀這個）
letters.json ──────────────────────────────┘        ▲
     ▲                                                │
build-letters.py ◀─ 字母卡 PNG + pearson-cuts.json ───┘（改 letters 後一定要重跑 units）

build-dialogue-list.py ─▶ ../Gogo English/圖片提示詞/dialogues.json（給生圖）
                        └▶ dialogues.json（網頁挑漫畫用）
build-images.py ─▶ images/*.webp（母版比 webp 新就重轉）
build-letter-audio.py ─▶ audio/letters/seg/*.mp3（TTS 備援）
review-letter-audio.py ─▶ Scribe 轉回文字對讀
```

### 3.4 音訊路徑（踩過最多坑的地方）
- 一般課 AI 語音：WebAudio `playbackContext`（24 kHz）→ `getOutputNode()`（喇叭模式走隱藏 `<audio>` 媒體路徑，避免 Android 通話模式導到聽筒）
- 字母旁白：**同一條 WebAudio 路**，教材整軌解一次、`source.start(0, offset, duration)` 播片段
- 瀏覽器只允許「使用者那一下」觸發的播放 → 第一段在 ▶ 開始的 click 裡同步播
- CSP：`media-src 'self' blob:`（`data:` URI 會被擋、外站音檔也會被擋）

---

## 4. 目前進度（v3.56）

### 4.1 已完成且上線
| 功能 | 版本 | 實機驗證 |
|---|---|---|
| 計畫驅動上課（辨識階梯、代換、對答） | v3.19–v3.37 | ✅ 多輪 |
| 239 張句型對話漫畫（泡泡由前端壓字） | v3.38 | ⚠️ 未專門驗 |
| 錯句修正（單複數、多空格、she/he、a/an…） | v3.39 | ⚠️ 未專門驗 |
| 第 2–5 天點選作答（程式判分） | v3.40, v3.45 | ⚠️ 使用者回報「講完沒反應」後加了提示跳動，未再驗 |
| 禁止每題結尾閒聊追問 | v3.42 → **v3.49 改寫** | ❌ v3.49 版未驗 |
| 字母 ABC 播放模式（不連線、每次整輪） | v3.42–v3.46 | ✅ 使用者 9/13「大致上沒問題」 |
| 字母旁白＝教材真人錄音 | v3.48 | ✅ 同上 |
| 字母切點起點提前（修字母名開頭被切掉） | v3.52 | ✅ 使用者 9/15「大致上沒問題」 |
| 字母課暫停／繼續（取代「換你唸」提示） | v3.53 | ❌ 未驗 |
| 字母課圖與聲音同步（課前下載整輪圖、換卡等圖好才唸） | v3.54 | ❌ 未驗（節流網路模擬過） |
| 字母每張卡固定等 1.5 秒 | v3.49 | ❌ 未驗 |
| AI 整輪沉默的偵測與重送 | v3.49 → v3.50 併入恢復預算 | ❌ 未驗 |
| 提示詞帳本（實際送出的指令可看、可匯出） | v3.50 | ⚠️ 只有瀏覽器模擬 |
| 提示詞按模式拆開（兒童計畫課移除舊規則） | v3.50 | ⚠️ 離線測試 |
| 回報識別碼 attemptId、去重、協定錯誤 | v3.50 | ⚠️ 事件重播測試 |
| 恢復預算、恢復畫面、求助鈕、動作橫幅 | v3.50 | ⚠️ 離線測試 |
| 判分寬鬆 | v3.50 | ❌ 未驗 |

### 4.2 數字
- 單元：38（27 正課 + 9 Review + 2 字母）
- 圖：`images/` 771 張 WebP（單字卡 527 + 對話漫畫 244）、`images/letters/` 52 張字母卡
- 對話漫畫覆蓋：v3.51 起代換題、對答題全部有圖（全單元五天實跑，缺圖 0）
- 回歸測試：`tests/run-all.html` 14 組全過（v3.50 加 lesson-flow、prompt-builder）

---

## 5. 開發歷程（時間線）

| 時期 | 版本 | 重點 |
|---|---|---|
| ~2026-08 初 | ≤ v3.13 | 階段式上課、GPT/Gemini 雙模型、時事討論、跨手機同步、下課偵測 |
| 08-18 前後 | v3.14–v3.16 | 計畫驅動架構三階段：回報工具 → 課前組清單 → 一次一項 |
| 08-24 | v3.17–v3.19 | 教材改讀 Gogo English 資料；**模板換成「辨識驅動」**；527 張本地圖卡 |
| 08-25～09-04 | v3.20–v3.37 | 實機一輪修一輪：插嘴、鬼打牆、跳針、結尾被剪、回報錯位、洩答、額度用完 |
| 09-06 | — | **電腦重灌**，對話紀錄遺失，從資料夾重建進度 |
| 09-08 | v3.38–v3.39 | 對話漫畫上線；揪出一整批程式組錯的句子 |
| 09-09 | v3.40 | 點選作答 |
| 09-10 | v3.41–v3.47 | 字母單元誕生 → 模型多話 → 播放模式 → **沒聲音連修四輪** → TTS 發音地獄 |
| 09-11 | v3.48 | 改用教材真人錄音，發音問題一次解決 |
| 09-13 | v3.49 | 「AI delay」其實是 GPT 整輪沉默；字母等待改 1.5 秒 |
| 09-14 | v3.50 | 穩定性重構：提示詞帳本、按模式拆提示詞、attemptId、流程控制器、恢復預算；補例句 |
| 09-14 | v3.51 | 補上最後 5 張對話漫畫，代換題全部有圖 |
| 09-15 | v3.52 | 字母真人音開頭被切（H 只剩 ch）→ 用波形量真正起音，28 張起點提前 |
| 09-15 | v3.53 | 字母課「換你唸」改成暫停鍵，繼續時整張卡重播 |
| 09-16 | v3.54 | 字母課唸 banana 圖還是 apple → 課前下載整輪圖、換卡等圖載好才唸 |
| 09-16 | v3.55 | v3.54 的預載用 img.decode() 會空等 15 秒 → 改用 load 事件 |
| 09-16 | v3.56 | 正式站 15 秒只下載到 9 張圖 → 照順序一次 3 張、前 6 張到就開始、音軌預載讓路 |

---

## 6. 問題全紀錄

格式：**症狀 → 根因 → 修法（版本）→ 教訓**。v3.36 以前的細節見 PROJECT-STATUS，這裡只列要點。

### 6.1 模型行為

| 症狀 | 根因 | 修法 | 教訓 |
|---|---|---|---|
| 同一句型連練八次 | 模型沒有「換題」的概念 | 句型家族上限＋導演指令換題（v3.12） | 限制要用程式執行，不是寫在提示詞裡請它守 |
| AI 自己說再見就下課；教 goodbye 那課形成迴圈 | 下課偵測認不出教材本身在教道別詞 | `setLessonTargets()` 遮蔽教材道別詞（v3.17） | 偵測規則要知道「今天在教什麼」 |
| 跳針：同一句連唸 20 遍 | 原生音訊模型不知道怎麼停；上下文重複密度高 | 切斷器（v3.24）＋合約改「講完就安靜等待」＋拔掉指令固定尾巴（v3.25） | 重複的輸入誘發重複的輸出 |
| 一開口就把答案（中文、拼法）講掉 | 指令裡就寫著答案 | 該藏的資訊不進指令，到揭露那一階才給（v3.28, v3.35） | 別一邊叫它不要說、一邊把答案塞它眼前 |
| 唸出提示詞 | GPT 兩發 response.create 對撞留下殘局 | 工具結果先送、指令的 create 當這輪回應，只開一發（v3.28） | |
| 每題結尾追問閒聊（21/21 題） | 合約只禁「額外例句」沒禁「追問」 | 合約補禁（v3.42） | ↓ 見下一條，這個修法本身出了大問題 |
| **「AI delay 非常嚴重」**（9/13） | **不是延遲**：46 輪裡 36 輪 AI 一個字都沒說。v3.42 寫「不准以問句結尾」，跟每題「問出來、等他答」打架，GPT 選擇閉嘴 | 合約改寫成「禁閒聊追問，出題的問句是必須的」＋`recoverSilentPlanTurn` 偵測空白輪重送指令（v3.49） | **規則之間不能打架**；「delay」先看 `aiTranscriptLength` 是不是 0 |
| 填字母判太嚴（說對 grandfather 被判錯） | 模型自己加了「要先講字母」等標準 | 指令寫死判斷標準（v3.37） | 仍待觀察：9/13 Rex 唸 playing 被判錯 4 次 |
| 字母課 AI 多話＋慢半拍（bear、cat 還沒唸就被推掉） | 模型天性＋重複回報推進計畫 | 字母單元整個拿掉模型（v3.42） | 不需要判斷的地方就不要用模型 |
| 模型把注音「ㄚ（阿）」當台詞唸 | 提示裡給了注音想請它「用中文解釋」 | 改給音標、明講用英文發音（v3.41） | 給模型的任何文字都可能被原封唸出來 |
| 導演筆記外洩（開場唸出 `[DIRECTOR NOTE - content…missing]`） | Gemini 偶發 | GPT 路徑已有靜音防護（v3.28）；Gemini 只觀察到一次 | **待觀察** |

### 6.1b 提示詞本身（v3.50）

| 症狀 | 根因 | 修法 | 教訓 |
|---|---|---|---|
| 不知道模型實際收到什麼指令、中途有沒有被改 | 系統提示在連線時組一次、中途 `updateInstructions` 沒記錄，診斷檔只有事件沒有內容 | 提示詞帳本：傳輸層送出那一刻快照，hash 去重，進診斷檔（v3.50） | 快照要在發送路徑取，不能事後重組冒充 |
| 兒童計畫課同時收到「只做這一項」和「回饋後再問一題」「同句型最多兩次」「提到名詞呼叫 show_image」 | 計畫合約是「串」在舊的回合契約與教學規則前面，舊規則沒拿掉 | 按模式組提示詞，兒童計畫課只留當前動作合約（v3.50） | 新架構上線時要把舊規則拔掉，不是疊在前面說「以這個為最高優先」 |
| 跳針偵測對所有 60 字以上的逐字稿都成立 | 正規表示式的反向參照 `\1` 在某次補丁被跳脫吃掉 | 補回並加測試（v3.50） | 測試只檢查「有這個函式」不夠，要真的餵正常與跳針的文字 |

### 6.2 回報與推進

| 症狀 | 根因 | 修法 |
|---|---|---|
| 答對還被重複問（鬼打牆） | 沒回報的問答被當成「答不出來」去爬階梯 | 兜底一律前進，階梯只由明確回報驅動（v3.20） |
| 新題目沒練就被跳過 | 上一項慢半拍的重複回報流到新項目頭上 | 回報 target 要對得上目前項目（v3.21）；回合分界防護（v3.20） |
| 上到一半跳掉 | 漏回報時兜底無聲推進 | 補問一次再兜底（v3.34） |
| GPT 卡在同一題 | 回報「e c l」（缺字母）比對不到 | 候選加入缺字母／拼法；偏離計畫先重送再跳過（v3.36） |
| 結語一個字沒說就斷線 | 結尾那輪空白 turnComplete 被當成講完；最後一題的回報流到結尾 | 結尾要有逐字稿才算；結尾不吃回報（v3.36, v3.37） |
| 點選題被模型搶著判分／兜底推進 | 模型看不到孩子點了什麼 | 點選題不吃模型回報、不兜底（v3.40） |
| 點選題「講完話沒反應」 | 模型被要求不回應，畫面在點之前沒動靜 | 講完話那一刻提示放大跳動（v3.45）＋頂端動作橫幅（v3.50） |
| 重複／遲到回報靠文字與時序猜，越補越多條件 | 回報沒有身分，只能比 target 字串；同一個字出現在兩題就混 | attemptId＋工具呼叫 id＋連線世代，文字只做核對（v3.50） |
| 沉默重送、補問、偏離重送、跳針、重連各自重試，疊起來「越修越多輪」 | 每條恢復機制各有自己的次數 | 共用恢復預算，用完進恢復畫面；兜底標 unverified 不算對錯（v3.50） |

### 6.3 程式組出來的錯句（v3.39，掛對話漫畫時整批浮現）
這些句子**會直接進導演指令唸給孩子聽**，配不到圖只是症狀。

| 錯句 | 根因 | 修法 |
|---|---|---|
| `Where's my socks?`、`Where are my skirt?` | overlay 只有單元層級 `plural`，混合單元表達不出來 | 新增 `wordPlurals`；句型各自帶 `plural` |
| `What do you do on go?` | `fillSlot` 把每個空格都換成同一個字 | 只填名字跟槽位相符的空格（`SLOT_ALIAS`） |
| `Which do you like, math or math?` | 兩個不同空格只有一個字 | 填不完整就交給模型 |
| `She's/He's my father.`、`Does she/he want…` | 擇一沒解開 | 改用對話漫畫上畫的那一句 |
| `It's [preposition] the [furniture].` | 對答題沒情境圖時直接用句型字串 | 改用漫畫的具體問答 |
| `Where's my sofa?` | 家具被當成隨身物 | `wordSlots` 標 `furniture` |
| 兩組問答黏成一句 | 情境圖兩組問答中間沒斜線 | `sceneQAPairs` 在句尾標點後再切 |
| `Is this a/an whale?` | 沒挑冠詞 | 填完自動挑 a/an |
| `swiming` | 產生器沒處理重複子音 | `ing()` |

**判斷方式**：全部單元跑一遍，句型題的 `target`/`ask` 不應出現 `[` 或 `a/b` 擇一。

### 6.4 音訊與播放（最貴的一段，9/10 一天修了四輪）

| 輪 | 使用者說 | 我以為的原因 | 真正的原因 |
|---|---|---|---|
| 1 | 完全沒聲音 | 自動播放政策擋掉（`new Audio()` 在 await 之後） | ✗ |
| 2 | 還是沒聲音 | 解鎖的那一下隔太多 await → 加 ▶ 開始同步播 | ✗ |
| 3 | 還是沒聲音 | `<audio>` 在那台裝置不出聲 → 改走 WebAudio | ✗ |
| **真相** | | | **`lesson-plan.js` 產生字母項目時沒帶 `audio`／`say` 欄位**，而且 `units.json` 是加欄位**之前**生成的、之後沒重跑。播放器每張卡拿到 undefined，靜靜走 2.6 秒安靜路徑，卡片照樣推進 |

**我怎麼被騙的**：在瀏覽器看到卡片照時間換，就當作「在播」。真的在播每張約 6.5 秒，靜音路徑約 3.5 秒——這個差別當時沒去量。測試全過是因為測試用自己的假資料。

**修法**（v3.45）：帶出欄位＋重建 `units.json`＋**新增直接讀 `units.json` 的資料測試**＋System Log 印「旁白預先解碼 N/N」。

**教訓**：
- 「有在動」不等於「有在做對的事」，要量
- 測試要有一條讀**真實產生物**，不能全用假資料
- 改了上游資料，下游產生物一定要重跑（`build-letters.py` → `build-units-from-gogo.py`）
- 使用者連續回報同一個症狀三次，代表我的診斷方向錯了，要退回去重查資料流，不是繼續加修補

**v3.54 圖跟不上聲音**（9/15 使用者：唸 banana 時圖還是 apple）
- 症狀：聲音已經換卡，畫面還是上一張的圖
- 根因：換 `<img>` src 後瀏覽器在新圖到之前繼續畫舊圖，播放器沒等圖就開唸；課前預載只是丟出請求、不等也不留參照
- 修法：新圖沒好先藏起來、載好才顯示；播放器等圖（最多 4 秒）再唸；▶ 開始之前先下載並解碼整輪 52 張（最多 15 秒）
- v3.55 追記：預載第一版用 `img.decode()`，它在分頁不在前景時永遠不回應，每堂空等 15 秒逾時。改用 load／error 事件。
  **修完要量「修法本身」花多久**——這次是推上線後重跑正常路徑才看到 ms: 15001
- v3.56 追記：正式站 GitHub Pages 很慢（單張 45 KB ~0.9 秒），52 張同時抓＋9 MB 音軌一起搶，15 秒只到 9 張。
  改成照順序一次 3 張、前 6 張到就開始、音軌預載等圖。**網路相關的修法要在正式站量，本機伺服器永遠是瞬間**
- 教訓：**「設了 src」不等於「畫面換了」**，跟「卡片有在換 ≠ 有在播」同一類——畫面與聲音要同步，就要等畫面真的好了；
  本機測試圖片瞬間到，這種問題只有慢網路才看得到，要用節流網路測

其他音訊問題：
- `stop()` 之後 `play()` 永遠不回來（等待中的 await 沒被放掉）→ v3.42
- CSP `media-src 'self' blob:` 擋掉 `data:` 靜音檔 → 解鎖改用真實檔（v3.43）
- 喇叭模式走聽筒 → 媒體路徑（早期）
- GPT turn completed 時語音還在播，按鈕太早解鎖把 AI 切斷 → 等 `output_audio_buffer.stopped`（v3.26）

### 6.5 字母發音（TTS 地獄 → 放棄 TTS）

| 輪 | 做法 | 使用者回饋 |
|---|---|---|
| v3.41 | 模型即時唸，給注音 | 模型把注音唸出來；而且話太多 |
| v3.42 | ElevenLabs 預錄，`A, a. aa. Apple.` | 沒聲音（見 6.4） |
| v3.43 | `A ... aa ... Apple.`，放慢 | 有些字母唸兩次、有些沒唸；太快 |
| v3.44 | 字母名用 phoneme 標籤（flash_v2） | A、E 字母名被唸成它的音 → 母音 phoneme 修好；**子音套 phoneme 反而壞**（H→A、N→and、S→say） |
| v3.46 | 試聽室：26 字母 × 3 種唸法讓人耳挑 | 選了 19 個，A E U R S X Z 三種都不行 |
| v3.46b | 第二輪：turbo_v2 ／ eleven_v3 原生 IPA | 選完；X、Z 要「A 引擎的名 + B 引擎的音」 |
| v3.47 | 拆成三段小檔，每段各選引擎 | — |
| **v3.48** | **使用者提議：用教材附的 QR 音檔** | ✅ 真人錄音，一次解決 |

**教材音檔**：培生數位資源頁（不用登入）每單元一軌「Alphabet: a, b, c」（軌 12/21/30/41/49/57/69/77/85），
內容正好是「A、/æ/、/æ/、apple」再留空。**不切檔**：每張卡記 `{file, start, end}`，
切點由 Scribe 逐字時間戳照位置算（每軌開頭 9 個字是說明，之後每張卡 4 個字），寫在 `pearson-cuts.json`。
音軌放進公開 repo 是**使用者 9/11 的決定**（選項 A）。

**v3.52 追記：切點開頭被切掉**（9/14 使用者：H 聽起來只剩「ch」）
- 症狀：部分字母卡一開始的字母名少了前半個音
- 根因：切點來自 Scribe 逐字時間戳，語音轉文字標的「字起點」比實際發聲晚 0.03～0.25 秒；52 張有 28 張起點落在聲音裡
- 修法：WebAudio 解碼整軌算音量包絡，找真正起音，start 改成起音 − 0.06 秒（v3.52）
- 教訓：**轉文字的時間戳只能當粗略位置，要切音檔就量波形**；本機 Python 沒有 mp3 解碼套件時，瀏覽器的 `decodeAudioData` 就能量

**教訓**：
- 我聽不到聲音，Scribe 轉文字又分不出「怪」——**發音只有人耳能判**，要盡早把判斷交給使用者（試聽室），不要一輪一輪猜
- 找現成的真人素材，常常比調 TTS 快得多
- TTS 保留為備援（`audio/letters/seg/`），`review-letter-audio.py` 仍可用

### 6.6 開發流程上我自己的失誤

| 失誤 | 後果 | 現在的做法 |
|---|---|---|
| v3.38–v3.41 只 commit 沒 push | 使用者看到線上一直是 v3.37 | **push 是進版的一部分**，推完要驗線上版本號 |
| 測試檔用 index 切片改壞 | 整份測試檔被截斷 | 從 git 還原；複雜修改寫成 scratchpad 的 `.py` 補丁腳本，用 `assert old in s` 確認錨點 |
| heredoc 裡的 Python 字串跳脫錯（`\\n`、regex） | 補丁寫進錯誤內容、測試 regex 語法錯 | 補丁腳本用 Write 工具寫成檔案再執行；測試避免在 JS 裡寫多行 regex |
| 不小心 commit `__pycache__/*.pyc` | repo 多了垃圾 | 已加入 `.gitignore` |
| 把使用者給的診斷檔當成最新的 | 分析到下午的舊資料 | 先看 `exportedAt` 與每場 `startedAt`、`appVersion` |
| 改了測試 html 但沒推快取版本號 | 瀏覽器用舊測試檔，測試「卡住」 | 每次改 `.js` 都推 `?v=` 版本號（index.html 與 tests/*.html） |

---

## 7. 工作守則（從上面整理出來的）

1. **先看資料再下結論**：診斷檔先看 `exportedAt`、每場 `startedAt`／`appVersion`／`model`，再算數字
2. **「delay」三步查**：(a) 學生講完到 AI 第一個動靜幾秒 (b) `ai_turn_completed.aiTranscriptLength` 是不是 0 (c) 有沒有 `plan_silent_turn`／`plan_report_nudged`／`plan_off_script_*`
3. **同一症狀回報第二次，就回頭重查資料流**，不要疊修補
4. **提示詞改動要檢查有沒有跟其他規則打架**（尤其「禁止 X」會不會擋到必須做的事）
5. **發音、聽感只能交給使用者**；做試聽頁比猜快
6. **產生物鏈要重跑到底**，並驗證最終上課讀的檔
7. **進版就要 push、驗線上、更新文件**（清單在 CLAUDE.md）

---

## 8. 未完成／待驗證／下一步

### 8.1 立即要驗（等使用者實機）
- [ ] **v3.50 穩定性重構實機一堂**（見 HANDOFF §3.1 清單）：口說答對、答錯一次、按「我不懂」、點選、中途斷網
- [ ] 診斷檔看：`plan_report_protocol_error`（模型有沒有照帶 attemptId）、`plan_silent_turn`、`plan_recovery_exhausted`、`item_result_summary.unverified`、`prompts.entries` 有無預期外的 update
- [ ] **v3.49 GPT 沉默修正**：匯出診斷檔看 `aiTranscriptLength=0` 的比例（上次 78%）、`plan_silent_turn` 次數
- [ ] 字母課 1.5 秒節奏是否剛好
- [x] **v3.52 字母切點**：使用者 9/15 聽過「大致上沒問題」
- [ ] **v3.53 字母課暫停**：孩子上課時按暫停／繼續，確認聲音立刻停、繼續從該張開頭重播
- [ ] **v3.54 字母課圖片同步**：用平常的手機網路上一輪，確認每張圖都跟著聲音換；匯出診斷檔看 `letter_images_preloaded`、`letter_image_timeout`
- [ ] 點選作答的實際體驗（孩子知不知道要點、提示跳動有沒有幫助）
- [ ] 對話漫畫在實際課堂的效果

### 8.2 已知但還沒處理
- [x] GPT 判斷偏嚴 → v3.50 合約改寬鬆標準（使用者定案），待實機驗
- [ ] 導演筆記外洩在 Gemini 出現過一次，觀察是否再發生
- [x] 代換題缺漫畫 5 句（B3U3 ruler／stapler ×2、B3U7 computer game）→ v3.51 使用者用 GPT 生圖、已上線
- [x] 78 個單字沒有例句 → v3.50 全補（`fill-word-examples.py`）
- [x] 句型缺逐句中文 → 查過 overlay，43 個句型的 `zh`／`answerZh` 都已齊（舊紀錄過時）
- [ ] 模型是否真的照帶 attemptId：若協定錯誤很多，考慮讓回報不帶 id 時用「目前作答」補（目前是拒收＋補問）

### 8.3 規格裡還沒做的
- [ ] 開場三種回應處理（準備好了／問英文問題／問無關的事）
- [ ] 學員弱點累積與運用（`report_item_result` 資料已在累積）
- [ ] 項目層級結果匯出到試算表
- [ ] 教材 `grammar`／`phonics`／`expressions` 欄位接進教學

### 8.4 保留備用
- Gemini 3.5 Transcribe（平行轉錄連線，可解發音稽核／拼字辨識）：使用者決定等真的遇到辨識問題再啟用

---

## 9. 使用者偏好與決策紀錄

**溝通**
- 繁體中文（台灣）
- 「不用停，除非一定要我確認」——能自己決定的就做完，真正需要使用者判斷的才問（例如版權、要不要公開）
- 要看到根因與證據，不接受「應該修好了」

**教學取向**
- 討厭 AI 多話：不要閒聊追問、字母課不要稱讚或評語
- 只練教材內容
- 字母課：每天整輪聽一遍，不接續、不分天；每張卡後等 1.5 秒；可以中途暫停（v3.53）

**已拍板的決策**（日期 → 內容）
- 08-24 辨識驅動模板；計畫模式預設開啟；延伸單字移除
- 08-27 Gemini 3.5 Transcribe 暫不採用
- 09-01 五天換模式；週曆制換單元；依上完堂數推進
- 09-03 發音單字進單字庫
- 09-09 點選作答由程式判定；第 1 天與代換維持口說
- 09-10 字母單元：字母→音→單字、不判對錯、兩個單元（順序／隨機）、播放模式、每次整輪、不標注音
- 09-11 字母用教材真人錄音；音軌放進公開 repo
- 09-13 字母每張卡等 1.5 秒
- 09-14 判分寬鬆；缺的例句直接補、事後給清單；缺的對話漫畫請 GPT 生圖
- 09-15 字母課要能中途暫停：「換你唸」提示改成「暫停」按鈕

---

## 10. 速查

```bash
# 本機伺服器（或用 Claude 的 preview_start name=ai-tutor）
py -3 -m http.server 8000 --bind 127.0.0.1
```

- 全部測試：<http://127.0.0.1:8000/tests/run-all.html>（標題變 ALLPASS 才算過）
- 教材 → `py -3 build-units-from-gogo.py`
- 對話漫畫清單 → `py -3 build-dialogue-list.py`；圖 → `py -3 build-images.py`
- 字母卡／切點 → `py -3 build-letters.py` → **再跑** `build-units-from-gogo.py`
- 字母切點微調：改 `pearson-cuts.json` 的 `start`/`end`
- TTS 備援旁白：`build-letter-audio.py`（`--only`、`--role`、`--force`、`--dry-run`）→ `review-letter-audio.py`
- ElevenLabs 金鑰在使用者環境變數（User 範圍），shell 讀不到時：
  `$env:ELEVENLABS_API_KEY = [Environment]::GetEnvironmentVariable("ELEVENLABS_API_KEY","User")`

---

## 附錄：版本變更一覽

| 版本 | 日期 | 一句話 |
|---|---|---|
| v3.56 | 09-16 | 字母卡圖照順序限量下載、前 6 張到就開始、音軌預載讓路（正式站 15 秒只到 9 張） |
| v3.55 | 09-16 | 字母卡預載改用 load 事件（decode() 會空等 15 秒） |
| v3.54 | 09-16 | 字母課圖跟不上聲音 → 課前下載整輪圖、換卡等圖載好才唸 |
| v3.53 | 09-15 | 字母課暫停鍵（取代「換你唸」），繼續時整張卡重播 |
| v3.52 | 09-15 | 字母真人音開頭被切 → 量波形起音，28 張切點提前 |
| v3.51 | 09-14 | 最後 5 張對話漫畫（B3U3、B3U7），代換題缺圖歸零 |
| v3.50 | 09-14 | 穩定性重構（帳本、按模式提示詞、attemptId、流程控制器、恢復預算、求助／恢復畫面）；判分寬鬆；補例句 |
| v3.49 | 09-13 | GPT 整輪沉默（規則打架）→ 合約改寫＋空白輪重送；字母等 1.5 秒 |
| v3.48 | 09-11 | 字母改播教材真人錄音（clip 切點） |
| v3.47 | 09-10 | 字母 TTS 拆三段、逐段選引擎 |
| v3.46 | 09-10 | 字母每次整輪、離開能回來、試聽室 |
| v3.45 | 09-10 | 沒聲音真正原因：項目沒帶 audio；點選題講完話畫面有反應 |
| v3.44 | 09-10 | ▶ 開始同步播；字母名 phoneme；Scribe 對讀 |
| v3.43 | 09-10 | 解鎖 audio；旁白每張唸字母、放慢 |
| v3.42 | 09-10 | 禁止追問；字母改不連線播放模式 |
| v3.41 | 09-10 | 字母 ABC 單元；發音改英文、不標注音 |
| v3.40 | 09-09 | 第 2–5 天點選作答 |
| v3.39 | 09-08 | 修掉程式組錯的句子 |
| v3.38 | 09-08 | 239 張對話漫畫上線 |
| v3.37 以前 | | 見 PROJECT-STATUS.md |
