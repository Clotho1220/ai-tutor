# 交接說明（新對話視窗請先讀這份）

最後更新：2026-08-09　目前版本：v3.17

這份文件記錄「**使用者定義的教學需求**」與「**目前做到哪、還沒做什麼**」。
技術細節與版本歷史在 [`PROJECT-STATUS.md`](PROJECT-STATUS.md)，兩份一起看。

---

## 0. 一句話現況

計畫驅動架構的階段 1–3 已完成並上線，但**實機尚未真正驗證過**——
上一次實測全程以為開啟了計畫模式，事後從診斷檔才發現開關是關的。
**下一步就是把開關打開、實際上一堂課、匯出診斷檔。**

---

## 1. 教學設計需求（規格來源）

使用者定義的目標架構：**把「決定下一步做什麼」從模型移回程式，模型只負責把每一步演出來**
（示範 → 詢問 → 提供練習 → 確認正確性 → 糾正 → 再示範 → 再練習）。

### 1.1 課前準備

上課前先整理好，讓 AI 明確知道今天要做什麼：

- 目前課程進度、過去學過的內容
- 學員程度
- 學員常見狀況（聽力較弱／語法較弱／發音不標準等）
- 今天要複習哪些單字與句型、要教哪些新單字、延伸單字、要練哪些句型

### 1.2 開場

- 說明目前上到哪、上過哪些、今天會上什麼（簡述）
- 問「`name`，你準備好了嗎？」然後等待
- 學員回應的三種處理方式：
  - 回「OK／I'm ready／Here we go」等 → 開始上課
  - 問英文相關問題 → 先回答該問題，回答完再問一次是否準備好
  - 問非英文的內容 → 簡短帶過，接著進行英文課程

### 1.3 複習

- **來源**：前一個 unit 與前前個 unit。這個 unit 學到的東西（含教材內與教學過程中延伸提到的單字），會在**接下來兩個 unit** 被複習到
- **分配**：上個 unit 若有 12 個單字，下個 unit 每天複習 2〜3 個
- **單字複習方式**：AI 說中文，學員用英文回答
- **句型複習方式**：同樣由 AI 說中文，學員說英文
- **糾正**：語法或用詞有誤時，要**明確指出哪裡不夠好**、提供正確句子，再讓學員說一次；仍不正確可再糾正並練習一次，但**同一句最多練兩次**
- 沒有可複習的內容就直接進入當天課程

### 1.4 當天課程

**單字**

- 第 1〜3 天：帶著學員練習單字英文唸法；全部說完後再複習一次（例如問「apple 是什麼？」或「蘋果的英文怎麼唸？」）。答錯就糾正並示範一次，再讓學員練習一次
- 第 4〜5 天：讓學員自己用英文唸出單字；全部唸完後讓學員拼出單字。忘記就糾正並示範一次，再練習一次
- **延伸單字**：每天針對當天單字找同類別的字延伸學習（例如學到 apple 可延伸 banana）。**一天只學一個新的**，且不可與過去學過的重疊

**句型**

- 所有句型都要練習過，方式為：
  1. 「你知道『你的名字是什麼？』的英文是什麼？請你試著用英文說說看」
  2. 「那如果人家問你『你的名字是什麼？』你應該怎麼回答呢？請你試著用英文說說看」
- 說錯就糾正並示範一次，再讓學員練習一次

### 1.5 結束

- 所有項目完成後，將該記錄的資料匯出到試算表，然後結束

### 1.6 已確認的設計決定

- **做完所有項目才下課**，時間僅作為護欄（使用者明確選擇）
- 同一題最多練習 **2 次**

---

## 2. 已實作 vs 尚未實作

| 需求 | 狀態 | 位置 |
|---|---|---|
| 課前組裝今日項目清單 | ✅ v3.15 | `lesson-plan.js` `build()` |
| 課前預覽（老師可先檢查） | ✅ v3.15 | 設定頁「🗒️ 今日課程計畫」 |
| 跨單元複習（前兩個 unit） | ✅ v3.15 | `course-progression.previousUnits()` |
| 12 個字分五天（2〜3 個／天） | ✅ v3.15 實測 3/3/2/2/2 | `spreadAcrossDays()` |
| 延伸單字納入複習 | ✅ v3.15 | 依學習紀錄的 unit 欄位回收 |
| 單字練習方式依天數切換 | ✅ v3.15 | `drill: pronounce / recall_and_spell` |
| 句型「問 + 答」兩種練習 | ✅ v3.15 | `pattern_ask` / `pattern_answer` |
| 延伸單字每天一個、不重複 | ✅ v3.15（選字交給模型，程式給種子與排除清單） | `extension_word` item |
| 結果回報工具 | ✅ v3.14 | `report_item_result` |
| 一次一項、依結果推進 | ✅ v3.16（**預設關閉**） | `createRunner()` + `sendCurrentPlanItem()` |
| 同一題最多兩次 | ✅ v3.16 結構保證 | `recordAttempt()` |
| 做完所有項目才下課 | ✅ v3.16 | 計畫跑完 → `scheduleLessonCompletion()` |
| **開場三種回應處理** | ❌ 未實作 | 目前只有一段 opening 指令 |
| **學員弱點累積與運用** | ❌ 未實作 | 需要先累積 `report_item_result` 資料 |
| **結束時完整匯出到試算表** | ⚠️ 部分 | 單字與句子練習有同步；項目層級結果尚未寫入 |

---

## 3. 立即該做的事

1. **⚙️ 設定 → 🗒️ 今日課程計畫 → 勾選「🧪 用計畫驅動上課」**
2. 開始連線後**先確認 System Log 第一行**：
   - 有開：`🗒️ 計畫驅動模式：Book 1 Unit 1 第 2 天，共 10 個項目`
   - 沒開：`📋 流程模式：依時間切換階段（計畫驅動未開啟）`
3. 上課中會看到逐項紀錄：`🗒️ [3/10] review_word：read`
4. 課後匯出診斷 JSON，重點看：
   - `plan_item_sent` / `plan_attempt` / `plan_completed`：計畫是否真的在推進
   - `plan_fallback_advance`：有多少是靠兜底推進（代表模型沒回報）
   - `item_result_summary.adherence`：模型的回報遵從率
5. 依遵從率決定下一步：
   - ≥ 0.9 → 直接做階段 4
   - 0.6〜0.9 → 加補問機制（偵測到練習卻沒收到回報時主動要求回報）
   - < 0.6 → 回頭檢討回報設計

---

## 4. 已知陷阱（踩過的坑，別再踩）

- **計畫模式預設關閉**。以為開著卻沒開，會誤判整輪測試結果
- **快取**：`index.html` 的模組版本已統一，改任何 `.js` 都要一起把版本號往前推；測試網頁同理，否則會出現「程式改好了但測試還是失敗」的假警報
- **版本號有兩處**：`app.js` 的 `APP_VERSION`（唯一來源）與 `index.html` 的 `#appVersion`（部署標記），必須同時更新
- **否定式提示會反效果**：曾在復原指令裡寫「不要說『我們還沒下課』」，模型反而照著講。一律改用正向敘述
- **教材本身在教 goodbye**（Book 1 Unit 1）會讓下課偵測誤判並形成迴圈——已於 v3.17 修正，改動該邏輯時要留意
- **Gemini 音色會飄**：已確認是模型端限制，前端無解，不要再回頭花時間
- **拼字用語音辨識不可靠**：孩子唸 `a-p-p-l-e` 幾乎一定被轉錯，第 4〜5 天的拼字練習需要另想呈現方式
- **發音正確性偵測不到**：語音轉文字會自動把發音正規化成正確單字，逐字稿看不出發音問題

---

## 5. 尚未驗證的假設

- 模型在計畫模式下是否真的乖乖一次只做一項
- `report_item_result` 的回報遵從率與判斷準確度
- 計畫驅動的節奏對 6–8 歲孩子會不會太機械
- GPT 在計畫模式下是否仍會「自己走自己的」（舊流程下確實會）

---

## 6. 常用指令

```bash
python -m http.server 8000 --bind 127.0.0.1
```

- 本機測試：<http://127.0.0.1:8000/index.html>
- 回歸測試：`tests/*-smoke.html`（11 組，最新 246 項全過）
- 更新教材：改 `Lesson data.xlsx` 後執行 `python build-units.py`
- Book 1 內容重建：`python tools-add-book1.py`（xlsx 未納入版控）
