# 交接說明（新對話視窗請先讀這份）

最後更新：2026-08-24　目前版本：v3.19

這份文件記錄「**使用者定義的教學需求**」與「**目前做到哪、還沒做什麼**」。
技術細節與版本歷史在 [`PROJECT-STATUS.md`](PROJECT-STATUS.md)，兩份一起看。

---

## 0. 一句話現況

v3.19 把上課模板整個換成「辨識驅動」（見第 1 節新規格），計畫模式已**預設開啟**，
教材資料與 527 張單字卡都來自「Gogo English」專案（見第 7 節）。
**下一步是實機上一堂課驗證新模板，匯出診斷檔。**

---

## 1. 教學設計需求（規格來源）

使用者定義的目標架構：**把「決定下一步做什麼」從模型移回程式，模型只負責把每一步演出來**。

### 1.1 v3.19 的教學模板（現行規格，2026-08-24 使用者定案）

起因：實際上課發現孩子**聽得懂 swim、看到 swim 這個字卻認不出來**。
使用者決定放棄舊的講解式架構，改成「針對教材內容複習」的辨識驅動模板，
所有練習只用教材裡的內容（延伸單字已移除）。

> **2026-09-01 更新**：單字練法已改為「不拆天、每天換模式」——
> 第 1 天中翻英、第 2 天英翻中（認字）、第 3 天三選一、第 4 天填缺漏字母、
> 第 5 天拼字，對齊紙本練習卷的五天題型；跨單元複習跟著當天模式走，
> 句型每天全練。詳見 PROJECT-STATUS v3.32。以下為 v3.19 的原始規格，保留供追溯。

**1. 單字（圖片辨識）** — `word_image`
用單字卡的圖問學員（如蘋果的圖問 What's this?），提示逐層給：
圖 → 答不出來秀中文 → 再不行秀英文（讓他自己讀字）→ 再不行唸給他聽、請他跟著唸
→ 唸得不標準糾正一次再唸一次。**最多糾正一次。**

**2. 單字（反向認字）** — `word_read`
只秀英文字，請學員唸出來並說意思；認不出來才給圖，再不行給中文。
這是針對「看不懂字」問題的直接練習。跨單元複習（前兩個 unit 的字）與
第 5 天的本單元總複習都用這一種。

**3. 句型代換** — `pattern_substitute`
依學員學過的內容練當週句型：當週句型 Can you sing?、過去學過 jump
→ 讓學員說 Can you jump?。以中翻英方式練，**每個句型至少三種**，
說不正確**最多糾正兩次**。代換字由程式挑（槽位相符、單複數相符的已學單字），
句子與中文提示由模型組（中文量詞跟名詞走，程式組不出來）。

**4. 句子對答** — `pattern_respond`
AI 用英文問（Can you fly?），學員要會答（No, I can't.）。
有句型情境圖就全程顯示，答案看圖決定，模型依圖判斷對錯。

**通用規則**
- 提示階梯＝嘗試次數，走完最後一階一定往下一項，不會卡住
- 圖片、英文、中文的顯示由前端控制，模型不呼叫 show_image、不先講還沒揭露的內容
- 開場（1.2）與結尾照舊保留

### 1.2 開場

- 說明目前上到哪、上過哪些、今天會上什麼（簡述）
- 問「`name`，你準備好了嗎？」然後等待
- 學員回應的三種處理方式（**尚未實作**）：
  - 回「OK／I'm ready」等 → 開始上課
  - 問英文相關問題 → 先回答，回答完再問一次是否準備好
  - 問非英文的內容 → 簡短帶過，接著進行課程

### 1.3 複習來源

- 前一個 unit 與前前個 unit 的字（含當時延伸教過的字），攤到五天，用 `word_read` 練
- 課本的 Review 單元不列入來源（它本身就是前三個單元的彙整）
- 沒有可複習的內容就直接進入當天課程

### 1.4 已確認的設計決定

- **換單元節奏＝週曆制**（2026-09-01 定案）：每週日換下一個單元；一週內照五天課程遞進，
  上超過五次重複第五天（拼字日），整週沒上則順延不跳過。詳見 v3.31。

- **做完所有項目才下課**，時間僅作為護欄
- 單字最多糾正 1 次、句子最多糾正 2 次（都由提示階梯的長度結構保證）
- 每個句型至少 3 種代換
- 只練教材內容，不延伸教材外的字
- Review 單元＝前三個正課單元的整理，Bonus / Culture 不上（使用者決定）

## 2. 已實作 vs 尚未實作

| 需求 | 狀態 | 位置 |
|---|---|---|
| 課前組裝今日項目清單 | ✅ v3.19 全面改寫 | `lesson-plan.js` `build()` |
| 課前預覽 | ✅ | 設定頁「🗒️ 今日課程計畫」 |
| 看圖說英文（提示階梯） | ✅ v3.19 | `imageLadder()` |
| 反向認字（字→圖→中文） | ✅ v3.19 | `readLadder()` |
| 句型代換（每句型 ≥3 種） | ✅ v3.19 | `pickSlotWords()` + `substituteItems()` |
| 句子對答（含情境圖） | ✅ v3.19 | `respondItems()` |
| 糾正次數上限（單字1／句子2） | ✅ 結構保證 | 階梯長度＝maxAttempts |
| 本地單字卡圖庫（527 張） | ✅ v3.19 | `images/` + `showCard()` |
| 前端控制揭露層級 | ✅ v3.19 | `revealFor()` + `applyPlanReveal()` |
| 跨單元複習（前兩個 unit） | ✅ | `course-progression.previousUnits()` |
| 課本 Review 單元 | ✅ v3.18 | 彙整前三單元 |
| 一次一項、依結果推進 | ✅ 預設開啟（v3.19） | `createRunner()` + `sendCurrentPlanItem()` |
| 做完所有項目才下課 | ✅ | 計畫跑完 → `scheduleLessonCompletion()` |
| 補問機制（漏回報先要求回報） | ✅ v3.34 | `planFallbackAfterTurn` 的 nudge |
| **開場三種回應處理** | ❌ 未實作 | 目前只有一段 opening 指令 |
| **學員弱點累積與運用** | ❌ 未實作 | 需要先累積 `report_item_result` 資料 |
| **結束時完整匯出到試算表** | ⚠️ 部分 | 項目層級結果尚未寫入 |
| 發音標準與否的判斷 | ⚠️ 只能靠模型 | 語音轉文字會正規化，前端無法稽核（見第 4 節） |

## 3. 立即該做的事

1. 計畫模式已**預設開啟**，不用再勾。開始連線後確認 System Log 第一行是
   `🗒️ 計畫驅動模式：… 共 N 個項目`（若顯示 `📋 流程模式` 代表被手動關掉了）
2. 上課中會看到逐項紀錄：`🗒️ [3/11] word_image：fly`，
   升階時顯示 `🔁 同一項升到第 2 階`
3. 實測重點（v3.19 新模板第一次上場）：
   - 圖片應該瞬間出現（本地檔）；出現「產生圖片中」代表走錯路徑
   - 模型會不會把還沒揭露的英文字先唸出來（提示階梯就毀了）
   - 句型代換的中文提示是否自然（那句是模型自己組的）
4. 課後匯出診斷 JSON，重點看：
   - `plan_item_sent` / `plan_attempt` / `plan_completed`：計畫是否真的在推進
   - `plan_fallback_advance`：有多少是靠兜底推進（代表模型沒回報）
   - `item_result_summary.adherence`：模型的回報遵從率
5. 依遵從率決定下一步：
   - ≥ 0.9 → 直接做階段 4
   - 0.6〜0.9 → 加補問機制（偵測到練習卻沒收到回報時主動要求回報）
   - < 0.6 → 回頭檢討回報設計

---

## 4. 已知陷阱（踩過的坑，別再踩）

- **計畫模式預設關閉**。以為開著卻沒開，會誤判整輪測試結果
- **快取**：`index.html` 的模組版本已統一，改任何 `.js` 都要一起把版本號往前推；測試網頁同理，否則會出現「程式改好了但測試還是失敗」的假警報
- **版本號有兩處**：`app.js` 的 `APP_VERSION`（唯一來源）與 `index.html` 的 `#appVersion`（部署標記），必須同時更新
- **否定式提示會反效果**：曾在復原指令裡寫「不要說『我們還沒下課』」，模型反而照著講。一律改用正向敘述
- **教材本身在教 goodbye**（Book 1 Unit 1）會讓下課偵測誤判並形成迴圈——已於 v3.17 修正，改動該邏輯時要留意
- **Gemini 音色會飄**：已確認是模型端限制，前端無解，不要再回頭花時間
- **拼字用語音辨識不可靠**：孩子唸 `a-p-p-l-e` 幾乎一定被轉錯，第 4〜5 天的拼字練習需要另想呈現方式
- **發音正確性偵測不到**：語音轉文字會自動把發音正規化成正確單字，逐字稿看不出發音問題
- **不要跑 `build-units.py` 或 `tools-add-book1.py`**：那是舊的 xlsx 產生器，會把 `units.json`
  覆寫回薄資料（第 2 冊每單元只剩 2 個字、沒有 Review）。v3.18 已加擋門，但別硬跑
- **句型的 `chinese` 是「用途說明」不是中文句子**，於是 `pattern_answer` 會產生
  「別人問你『用於詢問並表達喜愛的食物或事物。』的時候，你要怎麼回答？」這種聽不懂的提示。
  舊資料就有的問題，修法見第 7 節

---

## 5. 尚未驗證的假設

- 模型在計畫模式下是否真的乖乖一次只做一項
- `report_item_result` 的回報遵從率與判斷準確度
- 計畫驅動的節奏對 6–8 歲孩子會不會太機械
- GPT 在計畫模式下是否仍會「自己走自己的」（舊流程下確實會）

---

## 6. 常用指令

```bash
python -m http.server 8000 --bind 127.0.0.1
```

- 本機測試：<http://127.0.0.1:8000/index.html>
- 回歸測試：`tests/*-smoke.html`（11 組，v3.18 全過）
- 更新教材：先改「Gogo English」專案的 `教材資料/gogo{1,2,3}.json`，
  再回本專案執行 `python build-units-from-gogo.py`
- 更新圖庫：母版重生成後執行 `python build-images.py`（只轉新增的，`--force` 全部重轉）
- 一頁跑完全部測試：`tests/run-all.html`

---

## 7. 教材資料來源（v3.18 起）

教材的唯一真實來源是**另一個專案**：`../Gogo English/教材資料/gogo{1,2,3}.json`。
那份是由課本翻拍照片逐頁核對整理的，同時也給練習卷產生器與圖卡提示詞使用。

```
../Gogo English/教材資料/gogo{1,2,3}.json   ← 教材主體（單字、文法、發音、頁碼）
                    +
units-overlay.json                          ← 本專案人工整理的部分
                    ↓  build-units-from-gogo.py
units.json                                  ← 產生物，不要手改
```

**為什麼還要 overlay？** gogo 的 `target_sentences` 是具體例句（`It's a desk.`），
但 `lesson-plan.js` 的 `splitPattern()` 需要模板化句型（`What's this? / It's a/an [object].`）
才拆得出「問句 ／ 答句」。所以句型、英文 `theme` 與單字例句留在 overlay，改那裡不會被覆蓋。

### 這次帶進來、但還沒用的東西

| 欄位 | 內容 | 要用需要做什麼 |
|---|---|---|
| `grammar` | 每單元 2～3 條文法重點與中文說明 | 塞進系統提示詞，或做成講解型項目 |
| `phonics` | 第 1 冊 A–Z 各兩個字；第 2／3 冊音標 | `lesson-plan.js` 加一個 phonics 項目型別 |
| `expressions` | 課文裡的補充常用句 | 當作句型練習的延伸素材 |
| `pages` / `function` / `notes` | 課本頁碼、學習功能、教學提醒 | 課前預覽顯示，方便對照課本 |

### 已知待補

- **78 個單字沒有例句**（多半是第 2 冊新加的字）。例句是選填，但有的話示範會更穩。
  可以從 `gogo{1,2,3}.json` 的 `key_expressions` 或單元句型套字補上
- **句型缺逐句中文**。`gogo` 的 `target_sentences` 有 `{en, zh}` 一句一對，
  可以拿來補 `units-overlay.json` 裡句型的問句／答句中文，修掉上面第 4 節那個聽不懂的提示

### 保留備用的選項（使用者已評估、暫不採用）

- **Gemini 3.5 Transcribe**（2026-08 公開預覽，2026-08-27 評估）：語音轉文字模型，
  `gemini-3.5-transcribe-live` 走同一套 Live API，約 $0.009/分鐘（一堂課約 4 元台幣）。
  Custom vocabulary（可餵當天目標單字）＋ smart mode（拼讀轉乾淨文字）可以解掉
  「拼字語音辨識不可靠」「發音無法稽核」「學生逐字稿轉爛」這一排已知限制——
  做法是平行開一條轉錄連線，家教聲音不動。
  **使用者決定保留：先把教學流程調到最佳，等實際在辨識小朋友內容上出問題再啟用。**

### 那個專案還有什麼可以拿

- `圖片提示詞/圖片生成/`：527 張單字插畫母版（1254×1254 PNG，共 1.3 GB）。
  **v3.19 已接上**：`build-images.py` 轉成 512px WebP 放進本專案 `images/`（18 MB），
  計畫模式的圖片全走本地檔。母版重生成後重跑即可更新
- `progress.md`：兩組小孩的真實上課進度，可用來校準本專案裡各學員的目前單元
- `Bonus` 與 `Culture` 單元：目前刻意不納入課程流程（使用者決定）
