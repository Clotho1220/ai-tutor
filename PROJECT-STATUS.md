# AI Tutor Studio 開發進度

最後更新：2026-09-03  
目前版本：v3.37  
正式入口：<https://clotho1220.github.io/ai-tutor/>

## 1. 專案目標

AI Tutor Studio 是以 6–8 歲兒童為主要使用者的中英雙語語音家教，也提供 Clotho 使用的成人模式。課程以學生目前使用的 Gogo English 教材為主，AI 需要能聽學生說話、進行英文教學、修正文法、示範自然說法、顯示圖片，並跨天保存每位學生的學習進度。

目前學員：

- Rex：預設約 70% 英文。
- Jessie：預設中英文各半。
- Sandy：預設中英文各半。
- Clotho：成人英文對話模式。

每位學員會分別保存語言程度、AI 聲音、GPT 語速、目前單元、上課日期、單字及句子練習紀錄。

## 2. 目前可用功能

### 語音模型

- Gemini Live 3.1。
- GPT Realtime 2.1 mini，為較省費的 GPT 預設選項。
- GPT Realtime，保留為品質較高、費用較高的選項。
- Gemini 與 GPT 共用主要教學提示、課程階段、練習判斷及下課邏輯。
- 支援 Push-to-talk；按下說話時會中斷 AI，說完後才建立下一個回應。
- 可分別選擇 Gemini 與 GPT 聲音，GPT 可調整語速。

### 課程系統

- 教材來源為「Gogo English」專案的 `教材資料/gogo{1,2,3}.json`（由課本翻拍照片逐頁核對整理），經 `build-units-from-gogo.py` 產生 `units.json`，包含 Book 1、Book 2、Book 3 共 36 個單元（正課 27 + 課本的 Review 1／2／3 共 9 個）。
- Review 單元彙整前三個正課單元的句型與單字，不教新內容；單字五天平均攤開，且不再額外疊加跨單元複習（否則同一批字會被練兩次）。
- 每個單元自動展開成 5 天課程：前 4 天教新內容，第 5 天總複習。
- 每堂課長度依當天實際教學項目數決定，約 9-15 分鐘，並以 15 分鐘為上限；依序包含開場、主題／複習及總結階段。
- 第 5 天完成後，下次上課會自動前進到下一個單元。
- 可匯入自訂教材，包含主題、句型、單字、中文意思及例句。
- 可切換一般課程與時事討論。

### 教學行為

- 使用者用中文回答時，AI 會提供英文說法並停下來讓學生練習。
- 英文有錯時，AI 會自然重述正確句子，避免直接說「錯」。
- 英文正確時可提供替代表達，但兒童模式一天最多約兩次。
- 使用者說「你在說什麼／我聽不懂」時，AI 會簡化或解釋上一句，不會把它翻譯成 `What did you say?` 叫學生複誦。
- AI 邀請學生複誦後，系統會截斷後續內容，避免同一個回應又接下一題。
- GPT 會把 `I am happy / He is happy / Sandy is happy` 視為同一個句型家族；同一家族反覆出現時會動態要求 GPT 更換單字、情境、問題或角色扮演。
- 每個目標句原則上只進行一次模仿，必要時再修正一次；同一句型家族整堂課至多練習 2 次，達到上限時前端會以導演指令強制換題。

### 學生畫面

- 連線真正完成後才進入學生畫面。
- 可從學生畫面返回首頁，也可從首頁重新回到進行中的學生畫面。
- 顯示大型圖片、單字、Top 5 新聞題目及 AI 逐字稿。
- 畫面可以上下捲動，說話按鈕固定在上層。
- 圖片與文字使用內容版本／請求編號同步，降低上一張圖搭配下一個單字的狀況。
- 歡迎畫面使用 `tutor-welcome.webp`。

### 時事討論

- 後端從 Google News RSS 取得近期台灣及國際新聞。
- 學生畫面顯示 5 個可選題目，AI 不應替使用者自動選擇。
- 新聞只作為英文練習素材，不應變成長篇新聞播報。
- 每個主題會挑選 2 個單字及 1 個句型，並要求使用者表達自己的想法。
- 兒童模式會避開戰爭、死亡、暴力、犯罪、重大事故等不適齡內容。

### 學習記錄與跨手機同步

- 本機使用 `localStorage` 保存每位學員的設定及進度。
- Google Apps Script + Google 試算表用於跨手機同步。
- 試算表包含 `vocab`、`practice`、`state` 分頁。
- 保存單字、中文意思、例句、日期、次數、單元，以及中文轉英文／文法修正／替代表達紀錄。
- 不保存或上傳錄音。
- Gemini 與 OpenAI 正式 API Key 放在 Apps Script 的「指令碼屬性」，不寫入網頁或 GitHub。
- 手機端只取得短效憑證。

### 診斷與版本確認

- 設定頁顯示版本號，目前為 `AI Tutor Studio v3.37`。版本號的唯一來源是 `app.js` 的 `APP_VERSION`，`index.html` 的 `#appVersion` 為部署標記，兩處必須一起更新。
- 可匯出最近課堂診斷 JSON，內容包含模型、學員、單元、階段、逐字稿、延遲、工具呼叫及異常事件。
- GPT 診斷現在也包含單元名稱、預定時間與課程階段。

## 3. 最近完成的重要修正

### v3.37

2026-09-04 下午 13 場紀錄（v3.36 第一次上場）的修正：

- **Gemini 連續八次連不上**：不是網路。伺服器關閉理由是
  `Your prepayment credits are depleted`（AI Studio 預付額度用完），之後重連只會得到
  `Token has been used too many times`。額度類關閉理由加入「直接停下、跳出說明」的清單，
  不再重連三次。
- **最後一題的重複回報流到結尾頭上**：結尾項目沒有目標，任何回報都對得上，計畫立刻算完成、
  AI 一個字都沒說就下課（v3.36 的 closingSpoken 守衛被繞過）。改為結尾階段一律不吃回報，
  只靠 AI 真的講了結語收尾。
- **偏離計畫的誤判**：開場的慢半拍重複回報（「Are you ready?」「ready」）被當成偏離計畫，
  把 table 整題跳掉。改為「目前項目送出後孩子還沒說過話」或「上一項本來就沒有目標」的
  回報一律視為重複回報。
- **GPT 填字母判太嚴、話太多**：孩子說對 grandfather、說對 u t suit 都被判錯（理由是沒先講
  字母、順序、多唸一次），一題耗掉三階、每次都長篇解釋。指令寫死判斷標準（缺的字母說對
  或整個字唸對任一成立即 correct）、回饋一到兩句。挖空改為最多兩格（第 2、4 個字母），
  grandfather 不再挖五格。
- **GPT 逐字稿進診斷檔**：之前只有 Gemini 有，GPT 那堂「話很多」無從查證。

其他觀察：13:08 那場啟動失敗與多次「同步失敗」是 Apps Script 後端回應超過 15 秒；
13:30 那場第一句語音等了 12.8 秒（之後都在 1 秒內）。這些是後端／模型端的延遲，
使用者感受到的「網路很卡」主要來自這裡加上額度用完。

### v3.36

2026-09-04 兩堂實測（Gemini 一堂、GPT 一堂）的修正：

- **結語一個字都沒說就斷線**（Gemini）：結尾指令送出時會切斷模型手上那一輪
  （伺服器回 interrupted），那一輪空白的 turnComplete 被下課守衛當成「結語講完」，
  1.2 秒後直接斷線。改為結尾階段的 turnComplete 要有 AI 逐字稿才算講完；
  空白的記成 `closing_turn_empty` 診斷事件。
- **GPT 卡在同一題不推進**：pencil 答錯一次後模型回報「e c l」（缺的字母），
  比對不到「pencil」被當成重複回報丟掉；模型接著自己跑去教 Is it finished?、
  Can you sing?，每筆都對不上，計畫停在 pencil，孩子看著同一張圖抱怨。兩處修正：
  比對候選加入填字母題的缺字母／完整拼法／完整字；回報對不上且不是上一項的重複時，
  第一次重送目前項目把模型拉回來（`plan_off_script_resent`），再發生就跳過這一項
  往下走（`plan_off_script_skipped`）。
- **舊的「同句型練兩次就換題」機制在計畫模式停用**：它在 GPT 那堂把模型推去換題，
  脫離目前項目；計畫模式的重試上限由階梯控制。
- **填字母題 AI 先把英文字唸出來**（「它是 pencil 的意思」）：第一階指令改為用中文
  說意思、不唸英文字；判斷從寬，整個字唸對就算對（GPT 曾因「沒先講字母」判錯）。
- **開場加練**：AI 在開場要孩子複誦 Yes, I'm ready、又自己開始教 sing。開場指令改為
  孩子不管回什麼都算完成、立刻回報。

觀察到但這版沒動的：Gemini 每個項目的指令送出都會觸發一次 `ai_interrupted`
（指令切斷模型回報後那一輪），目前逐字稿看起來內容完整，先記錄；Gemini 的學生逐字稿
偶爾被辨識成韓文／西班牙文（「에에에 banana」），判斷靠原生音訊不受影響，只是紀錄難讀。

### v3.35

2026-09-03 實測回饋的五項修正：

- **填字母／拼字一開口就洩答**：指令把完整拼法與缺的字母都給了模型。改為第一階
  不給答案（模型自己認得這些基礎字，聽孩子說完再判斷），第二階起才給拼法供示範。
- **情境圖配錯句型**（Who's she? 配到男孩、What's that? 配到奶奶而教出 It's a grandmother）：
  原本用索引輪流配圖。改為解析情境圖的 lines（「問 — 答」對），只配「問句開頭對得上」
  的圖，並直接採用圖上那一組問答當題目；沒有對應圖就不顯示圖。
- **句子練習畫面加中文**：`units-overlay.json` 每個句型新增 `zh`（代換句中文模板，
  【】填入代換字的中文）與 `answerZh`（對答提示）。代換題顯示「fly／你會飛嗎？」，
  對答題顯示「Can you swim?／會，我會。／不會，我不會。」＋情境圖。
- **發音（Alphabet）單字進單字庫**：`units.json` 每單元新增 `phonicsWords`
  （158 個字、全部有圖），與課本單字一起進每日練習；不進句型代換池。
- **連線不穩**：診斷檔證實是 Apps Script 後端短暫故障（Failed to fetch、回 HTML 錯誤頁、
  同步逾時）而非 App 或 AI 伺服器。Gemini 與 GPT 的憑證請求改為失敗重試兩次（間隔 2 秒）。

### v3.34

- **補問機制（HANDOFF 階段 4 規劃的功能）**：「上到一半跳掉」的根因是
  模型漏回報時兜底機制直接無聲推進（2026-09-02 實測 hello、hi 兩項就這樣被跳過，
  遵從率 0.7~0.9 正落在規劃的「加補問」區間）。現在漏回報時先安靜地發指令
  要求模型「立刻為剛才的嘗試回報結果」，並重申目前項目（模型岔題亂跑時
  順便拉回來）；補報了就拿到真實對錯，補問後仍不報才兜底跳過。
  每項最多補問一次；開場與結尾不補問。診斷事件：plan_report_nudged。

### v3.33

- **天數推進改依「上完的堂數」**（2026-09-01 使用者定案）：同一個日曆天上兩堂
  就是兩天的內容，不再以日期為準。保護：上一堂要「正常上完」（下課收尾）才算——
  中斷、連線失敗、手動提早結束的場次不推進，下次重上同一天
  （當天實測就有一場 21 秒的憑證逾時失敗，不能讓它燒掉一天）。
  第 6 堂起重複第 5 天（拼字日）；每週日換單元照舊。

### v3.32

- **五天課程改為「單字不拆天、每天換模式」**（2026-09-01 使用者定案，
  對齊紙本練習卷的五天題型）：每天練整個單元的字，模式依天遞進——
  第 1 天中翻英（圖＋中文→說英文）→ 第 2 天英翻中（認字）→
  第 3 天三選一（畫面三個英文字，AI 說中文，孩子唸出正確的；AI 被禁止唸選項）→
  第 4 天填缺漏字母（畫面顯示 d_s_ 挖空版）→ 第 5 天拼出單字。
- 跨單元複習的字**跟著當天模式走**，整堂課同一種規則。
- 句型也不拆天：每個句型每天都練（每句型 2 種代換；單句型的單元 3 種）。
- 例外：課本 Review 單元字數是正課三倍，仍五天攤開（模式照天走）、句型每天輪 2 個。
- 新項目型別 word_zh2en / word_choice / word_gap；三選一的誘答選項取自同批
  slot 相符的字；填字母的遮罩規則為「保留首字母、其後隔一挖一」（sing → s_n_）。
- 完成卡片對三選一顯示正確答案（非三個選項）、對填字母顯示完整字。
- 延伸單字（learnedWords）照舊併入複習池，跟著當天模式練。

### v3.31

- **換單元節奏改為週曆制**（2026-09-01 使用者定案）：每**週日**強制換下一個單元，
  不再等五天上完。一週內仍照五天課程遞進（1~3 看圖、4 認字、5 拼字）；
  同週上超過五次就重複第五天；整週沒上課則順延一個單元（不跳過沒看過的內容）。
- 週錨點（unitWeek）跨手機同步——否則兩支手機各自在週日推進會連跳兩個單元。
  跨週後舊的天數進度不延續；手動指定第幾天的功能照舊可用（測試／補課）。
- 舊的「五天上完自動進下一單元」邏輯退役（shouldAdvance 保留於模組供測試）。

### v3.30

- **診斷保留 3 筆 → 20 筆**（使用者一週才匯出一次，一天可能上 2~3 堂）。
  每場約 60~150KB，20 筆約 2~3MB。localStorage 配額不夠時 `saveHistory`
  會自動折半減量（丟最舊的），保證最新一場一定存得進去；已用模擬配額單元測試。

### v3.29

2026-08-31 兩場 v3.28 實測是歷來最乾淨的（零錯誤、零跳針、零外洩、回報 9/9、
提示階梯全程照設計走）。剩最後一個缺陷：

- **佔位符目標的回報比對**：對答題的目標是模板（`It's a/an [object].`、`I'm [Name].`），
  孩子回報的是填好的句子（It's a goodbye sign.），逐字比對永遠對不上——
  兩場各有 2 筆正確回報被丟掉、項目只能靠兜底推進，答錯時提示階梯也不會啟動。
  改為：可核對的候選（問句等）先比對；全都對不上而目標含佔位符時，
  只要不是「上一個完成項目」的重複回報就接受。已用實際 session 的
  六種案例（含反例）以 Node 單元測試驗證。

### v3.28

2026-08-30 兩場 GPT 實測（唸出提示詞、先講中文）的修正：

- **模型唸出提示詞＝回應對撞的後遺症**：回報工具會推進計畫並送下一項指令
  （自帶 response.create），與工具結果的 response.create 同時開兩發，
  後到的被伺服器拒絕（一場 5 次），對話留下沒被回應的工具輸出，
  模型下一輪面對殘局就開始亂唸。改為：指令先暫存、工具結果先送（不開回應）、
  有指令就讓指令的 create 當這一輪的回應——全程只開一發。
- **GPT 路徑補上導演指令外洩防護**（Gemini 本來就有、GPT 漏了）：
  偵測到逐字稿出現 DIRECTOR NOTE 即靜音本輪剩餘語音並裁掉字幕。
- **word_read 一開口先講中文**：指令裡一直寫著「中文是你好」——
  一邊叫模型別說、一邊把答案塞它眼前。改為該藏的資訊不進指令：
  中文意思只在提示階梯揭露中文的那一階才給，早期階段並明文列出
  「孩子開口前不可說出的部分」。

### v3.27

2026-08-29 兩場實測（GPT 卡死、Gemini 結尾又被剪）的修正：

- **GPT 中途卡死**：一場在第 8 項後伺服器事件完全消失（資料通道無聲死亡），
  孩子連說五次話毫無回應、前端毫無提示，只能手動關掉。新增看門狗：
  孩子說完話後 15 秒內完全沒有伺服器事件，就明確顯示「連線可能已中斷，
  請按結束連線重新開始」並解鎖按鈕。
- **GPT active response 錯誤 ×6**：v3.22 只給 sendText 加了防撞守門，
  漏了 sendToolResult（回報工具的回覆也會開新回應）。兩處同守門，
  且真的送出時清掉 pending 避免重複 create。
- **Gemini 結尾道別又被剪**：模型把最後一題的稱讚和結尾併成同一輪講掉，
  此時結尾指令還在排隊、正式下課旗標未立，道別被誤判「提早」而剪音訊。
  改為：計畫已走到結尾項目時的道別一律視為正式（旗標補立、排隊指令作廢）。

### v3.26

2026-08-28 GPT 場實測的五項修正：

- **句子練習講到一半沒聲音（Gemini）**：舊流程的「複誦邀請截斷器」在計畫模式誤傷——
  AI 說到「試試看」，後面的說明音訊全被前端靜音。計畫模式停用音訊截斷（偵測保留給統計）。
- **句子練習講到一半被切（GPT）＋按鈕太早解鎖**：同一根因——GPT 的 turn completed
  只代表生成完畢、語音還在播，按鈕卻已解鎖，孩子一按就把 AI 切斷（診斷檔的 truncate
  錯誤即此）。按鈕改在 output_audio_buffer.stopped（播放真正結束）才解鎖。
- **糾正後叫孩子再練、下一秒卻跳下一個字**：AI 邀請重練的那一輪完成時，兜底照樣推進。
  改為該輪偵測到練習邀請（practiceRequested）就不兜底，等孩子真的練那一次。
- **句型代換畫面一片空白**：代換題現在顯示要代換的單字（英文＋中文）——
  句子結構才是考點，單字是合理的鷹架。
- **稱讚跳針（太棒了 ×N）**：人設加入「稱讚要輪替、不得連續同一句」。
  註：這批診斷檔沒有 Gemini 場，若下次 Gemini 還是重複，看 System Log 有沒有
  「🔂 偵測到 AI 跳針」——有代表切斷器有動作、門檻可能要調；沒有代表是逐輪的習慣性稱讚。

### v3.25

跳針的根因處理（v3.24 的切斷器退居第二道防線）＋教學情境重寫：

- **跳針根因一：模型不知道怎麼「停」**。v3.22 要求它講一句就停，但原生音訊模型
  收不了尾時會用重複去填。TURN CONTRACT 與 PLAN MODE 合約改為正向指令：
  「講完就結束你的回合、安靜等待——安靜等待是好教學的一部分，永遠不要用重複自己來填補沉默」。
- **跳針根因二：上下文重複密度太高**。每個項目指令的固定尾巴
  （「只做這一件事…記得回報…」一字不差、每堂 9 次以上）拔掉，
  通用規則只在系統提示講一次。重複的上下文會誘發重複的生成。
- **教學情境依使用者 2026-08-28 定案的架構重寫**（兒童模式）：
  有耐心、給情緒價值的老師 → 目的（針對課程內容口說練習、糾正發音與句構、
  同一單字／句子最多糾正兩次）→ 四步流程（引導 → 判斷 → 糾正示範再練 → 稱讚推進）。
  開場白改為「上次學了什麼 → 今天學什麼 → Are you ready?」；
  結尾改為「今天學到什麼 → 我們下次再見囉, bye bye!」。
  成人模式不變；語言比例照舊由學員程度設定驅動（languagePolicy）。
- 重置學習紀錄按鈕在清完雲端後**立刻拉回驗證**是否真的清空；
  後端還是舊版 sync.gs 時明確提示要先重新部署。

### v3.24

2026-08-27 兩場實測（都是 Gemini）的修正：

- **AI 跳針切斷器**：原生音訊模型偶爾陷入退化重複——實測一場把同一句連唸
  20 幾遍（28 秒、1000 多字），兩場都發生。模型端擋不了，前端改為監看逐字稿：
  同一段文字（8 字以上）連續出現 4 次即切斷該輪語音、丟棄殘餘音訊，並立刻重送
  目前項目的指令（新輸入會讓 Gemini 中止跳針中的生成）。門檻取 4 次是為了
  不誤殺拼字示範（s-w-i-m ×3）這類正常重複；已用兩場實際跳針文字驗證偵測器。
  每堂最多主動復原 3 次，避免模型狀態異常時火上加油。
- **AI 沒說再見就斷線**：v3.20 的指令排隊讓「正式下課」旗標在結尾項目排隊當下
  就立起，下課守衛把前一題的回饋語音誤當告別，1.2 秒後斷線——兩場都是
  這樣結束的。旗標改為在結尾指令「實際送出」時才立，告別會真的說完。

### v3.23

- **移除自訂教材功能**（使用者決定：教材已統一走 Gogo English 資料源，用不到了）。
  設定頁的貼上面板、解析與教案組裝程式全數移除；原本放在該面板裡的
  「興趣」欄位保留，移到獨立區塊（AI 開場閒聊與造句會用到）。
- **新增「🧹 重置學習紀錄」**（同步面板底部）：清除所有學員的單字紀錄、
  句子練習與各單元進度（回到第 1 天），學員名單、程度、聲音與選定單元保留。
  按兩次才執行；有設定同步時一併呼叫後端新的 `wipeRecords` 動作清空試算表。
  **注意**：每支有舊資料的手機都要各按一次，否則下次同步會把舊資料傳回雲端；
  `sync.gs` 有改動，要重新部署（部署 → 管理部署作業 → 編輯 → 新版本）。

### v3.22

第三輪實測（三場：Gemini×2、GPT×1）的修正：

- **AI 說話時鎖住說話按鈕**（使用者要求）：孩子常在 AI 沒講完就按，把題目攔腰切斷。
  AI 回合開始即鎖、turn completed 解鎖，30 秒保險絲防卡死。鎖住時按鈕顯示「🔈 老師說話中…」。
- **嚴格一來一回**：Gemini 每個字都自行加碼「You can say "I watch TV." Try it!」，
  多出來的回合讓兜底誤推進（一場 7 次）、回報錯位（5 筆被忽略）。系統提示與
  單字階梯指令都明令：一問一答一句回饋就停，不得追加例句、造句或 Try it。
- **答對亮出完整卡片**：word_read 答對時從頭到尾看不到圖（第一階刻意只給字練認字），
  被誤認為圖片壞了。現在項目完成時顯示圖＋英文＋中文當作確認與回饋。
- **GPT「active response in progress」錯誤 ×8**：sendText 在回應進行中仍直接
  response.create。改為掛起（responseCreatePending），等 response.done 再補。

### v3.21

第二次實測（v3.20、Clotho 自測）遵從率大幅改善（10 筆回報、僅 1 次兜底），
但暴露「有些字沒練到就被跳過」，並依使用者要求加入拼字：

- **回報目標比對**：模型會對同一項回報多次（第一次唸＋複誦各一筆），慢一拍的
  那筆流到下一個項目頭上，新項目沒練就被推進。現在回報的 target 要與目前項目
  對得上（雙向包含）才推進，不符者記為 `plan_report_ignored`，由兜底機制接手。
- **拼字練習（`word_spell`）**：整週遞進改為「第 1~3 天看圖會說 → 第 4 天看字會唸
  → 第 5 天會拼」。拼字階梯：憑記憶拼（只給圖＋中文）→ 看著字拼 → AI 逐字母示範跟拼。
  指令附上 `f-l-y` 逐字母參考。對錯只能靠原生音訊模型聽（語音轉文字會把拼讀轉爛，
  前端無法驗證——與發音判斷同一信任模式）。

### v3.20

首次實機測試（Gemini、手機、Sandy）暴露三個問題，全數修正：

- **AI 插嘴、語音被攔腰切斷**：導演指令一送出，Gemini 就中斷手上的語音。
  推進與升階原本立刻送指令，會切斷 AI 正在講的回饋，或疊在孩子說到一半的聲音上。
  改成指令排隊：AI 在講或學生在說時先排隊，該輪自然結束才送。
- **答對了還被重複問（鬼打牆）**：模型的回報遵從率實測很低（整堂 1 次），
  沒回報的問答被當成「答不出來」去爬提示階梯，階梯指示都是「他不會，給提示再問」。
  改成兜底（unknown）一律直接前進，提示階梯只由明確回報的 incorrect / no_response 驅動。
- **幽靈嘗試**：回報推進後 25 毫秒內，屬於上一項的 turn completed 把全新項目打成
  一次失敗並升階。加上回合分界防護：項目指令送出前就開始的問答不計數。
- **圖片慢半拍**：課前把當天所有單字卡（每張約 35KB）預載進快取；
  計畫模式不再向 Pollinations 預繪圖片。
- 開場指令補上「問完準備好了嗎就停，不要自己開始教」；每個項目指令
  尾端提醒回報 report_item_result（拉高遵從率）。

### v3.19

- **上課模板整個換掉**（使用者決定放棄舊的階段式架構）。起因是實際上課發現孩子
  聽得懂 swim 卻看不懂 swim 這個字——舊模板全程講解與跟讀，從來沒讓她讀字。
  新模板是「辨識驅動」：每一項都先讓學員產出，答不出來才逐層給提示。
- 四種項目型別（`lesson-plan.js` 全面改寫，計畫版本 version: 2）：
  - `word_image` 看圖說英文：圖 → 加中文 → 加英文（要求自己讀字）→ 示範跟讀 → 糾正一次
  - `word_read` 看英文字說意思：字 → 加圖 → 加中文 → 跟讀。專練認字的反向練習
  - `pattern_substitute` 句型代換：拿學過的同槽位單字換進句型（Can you sing? → Can you jump?），
    每個句型至少三種；說錯最多糾正兩次
  - `pattern_respond` 聽問題答句子：AI 問 Can you fly?，學員答 No, I can't.；有句型情境圖就全程顯示
- 提示階梯＝嘗試次數：同一個計數器同時決定「練了幾次」與「提示揭露到第幾層」，
  走完最後一階一定往下走，結構上不會卡住。
- 學生畫面改由前端控制：`student-view.js` 新增 `showCard()`，圖片、英文、中文由
  計畫的揭露層級決定，模型在計畫項目中不再呼叫 `show_image`。
- **527 張本地單字卡上線**：`build-images.py` 把「Gogo English」專案的插畫母版
  （1254×1254 PNG、1.3 GB）轉成 512px WebP，全部 18 MB，放在 `images/`。
  計畫模式的圖片改走本地檔，即時生圖（Pollinations）只剩舊流程與時事模式在用。
- `units.json` 每個單字新增 `slot`（句型代換的語法類別）、`image`（圖檔名）、
  `askType`（這張圖適合問哪種問題）；每個單元新增 `scenes`（句型情境圖）。
  複數單元（第 2 冊 U6／U11、第 3 冊 U10）的字與句型帶 `plural`，
  代換時單複數互不混用，避免組出 They're cake。
- **計畫模式改為預設開啟**，設定頁開關反轉為「關閉則退回舊的階段流程」。
  時事模式與未選單元時仍自動回舊流程。
- `report_item_result` 的 `kind` 列舉同步改為新的四種項目型別。
- 延伸單字（每天教一個教材外的同類字）依使用者決定移除：只複習教材內容。
- `tests/lesson-plan-smoke.js` 全面重寫（54 項），新增 `tests/run-all.html`
  一頁跑完全部 11 組測試。

### v3.18

- 教材資料改用「Gogo English」專案的整理成果。原本的 `Lesson data.xlsx` → `build-units.py`
  已停用，改為 `build-units-from-gogo.py`：教材主體讀 `../Gogo English/教材資料/gogo{1,2,3}.json`，
  本專案只保留 `units-overlay.json`（人工整理、gogo 沒有的部分）。
- 正課單字由 126 個增為 200 個。差最多的是第 2 冊：原本每個單元只有 2 個單字，
  現在是 8～12 個。原本的字數湊不出「上個單元 12 個字、下個單元每天複習 2～3 個」的設計。
- 納入課本的 Review 1／2／3（每冊 3 個，共 9 個單元），單元數由 27 增為 36。
  Review 單元的教法在計畫模式與原流程都改成複習（AI 說中文、學員說英文），不做示範跟讀。
- `units.json` 新增 `grammar`（文法重點與說明）、`phonics`（自然發音）、`expressions`
  （課文補充常用句）、`pages`、`function`、`notes` 欄位。目前只存放，尚未接進教學流程。
- 舊的 `build-units.py` 與 `tools-add-book1.py` 加上停用擋門。兩者都會把 `units.json`
  覆寫回舊的薄資料，這次整理過程中就誤觸過一次。

### v3.17

- 修正「教到 goodbye 就被誤判成下課」造成的失控迴圈。當天目標單字若是 goodbye（Book 1 Unit 1 Day 2），AI 每次講到它就被判定為提早道別，音訊被切、送出復原，而復原又產生相同內容，實測 80 秒內連續觸發 6 次、課程卡死。
- `lesson-ending.js` 新增 `setLessonTargets()`：教材本身在教道別詞時，課中一律不以該詞判定下課，只有結語階段才承認；判定前一併遮蔽引號內容。
- 早退復原加上 2 次上限，連續失效即停止介入。
- 計畫模式未開啟時會明確寫入系統紀錄與診斷 metadata（`planMode`）。先前一次實測全程以為在跑計畫驅動，實際開關是關的。
- `index.html` 各模組的快取版本統一為單一版本，避免改了模組卻忘記調整版本而載到舊檔。

### v3.16

- 新增計畫驅動上課（階段 3），以設定頁的「🧪 用計畫驅動上課」開關啟用，**預設關閉**，可與原本的階段流程並行比較。
- 上課時一次只送一個項目給模型，依結果推進。「同一題最多兩次」由執行器保證：答對立即前進、答錯留在原地重練、第二次不論對錯一定前進，並標記 `needs_review` 供日後複習。
- 推進條件為「回報優先、對話輪替兜底」，因此模型的回報遵從率只影響判斷精準度，不影響流程能否進行。
- 計畫跑完即下課；結尾項目會通知下課守衛此時的道別是正式的。時事模式與未選單元時自動回到原流程。
- 系統提示詞在計畫模式加上 PLAN MODE 合約：一次只做被指定的那一項，不得自行加練、跳題或決定下課。
- 所有測試網頁補上版本參數。測試檔被瀏覽器快取時會出現「程式已修好但測試仍失敗」的假警報。

### v3.15

- 新增 `lesson-plan.js`（計畫驅動架構的階段 2）。課前就把今天要做的每一件事算成明確清單：複習哪些單字與句型、教哪些新單字、延伸一個什麼字、練哪些句型，每個項目都帶兩次上限與待辦狀態。
- 複習排程落實跨單元設計：前兩個單元的單字平均攤到五天，當時延伸教到的字也一併排入；句型輪流複習，確保每天都練到。
- 單字練習方式依天數切換：第 1-3 天帶著唸，第 4-5 天自己唸再拼字；第 5 天改為整個單元總複習。
- 設定頁新增「今日課程計畫」預覽，上課前可先檢查今天的項目清單。目前尚未接手上課流程。
- 修正句型拆解：改以「空白 / 空白」為分隔符，原本用裸斜線會把 `she/he`、`his/her`、`a/an` 這類句型內部的斜線誤判；`pattern_answer` 改為保留單一目標句並將其餘答法放進 `alternatives`。

### v3.14

- 新增 `report_item_result` 工具（計畫驅動架構的階段 1）。模型每次判斷完學生的嘗試後必須回報目標、結果、實際聽到的內容、第幾次嘗試與項目類型。本階段只收集與觀察，尚未改變上課流程。
- 課程結束時輸出 `item_result_summary`：回報筆數、前端偵測到的練習邀請次數、遵從率、結果分布與重試次數，用來驗證模型是否穩定回報。
- 工具宣告收斂為單一來源。原本 GPT 與 Gemini 各自維護一份，屬於先前提示詞分歧的同一類風險；現由 `tutorToolDeclarations()` 產生，`geminiToolDeclarations()` 負責轉成 Gemini 的大寫型別。

### 未來規劃：計畫驅動架構

目標是把「決定下一步做什麼」從模型移回程式，模型只負責把每一步演出來。

- 階段 1（已完成）：`report_item_result` 與遵從率觀察。
- 階段 2（已完成）：課前組裝 `LessonPlan`，並提供課前預覽。
- 階段 3（已完成，預設關閉）：改為計畫驅動，一次送一個項目；同一題最多兩次已是結構保證。
- 階段 4：開場的三種回應處理、跨單元複習排程（本單元的字在後兩個單元複習）、弱點累積。
- 已確認的設計決定：**做完所有項目才下課**，時間僅作為護欄。

### v3.13

- 下課時改為等結語真的播完再斷線。`ai_turn_completed` 只代表模型產生完畢，語音仍在播；GPT 走 WebRTC 播放，前端估不出剩餘音訊，舊版因此永遠落到 350 毫秒下限，結語幾乎整段被切掉。
- `openai-realtime.js` 對外提供 `isSpeaking()`，並在 `output_audio_buffer.stopped` 時發出事件，作為播放結束的可靠訊號；下課最長等待 25 秒。
- Gemini 路徑仍依前端排程估算剩餘音訊，但下限由 350 毫秒提高到 1200 毫秒。

### v3.12

- 課程長度改依當天實際項目數估算（新教學每項約 2.5 分、複習每項 1.5 分），並加上 15 分鐘總長上限。原本固定 11 分鐘的主課階段在項目很少的日子（例如 Book 1 第 1 天僅 2 項）會讓模型無素材可教，只能反覆操練同一句。
- 同一句型家族的練習上限由「三個連續回合」收緊為「整堂課至多 2 次」，並改以導演指令硬性換題。診斷檔顯示舊的 `updateInstructions` 附加作法觸發 8 次仍被模型忽略。
- 上述限制改為兩個模型共用，Gemini 先前完全沒有這層保護。

### v3.11

- 匯入 Gogo Loves English 1（Book 1）共 9 個單元，課本單元庫由 18 個增為 27 個。
- 修正 `APP_VERSION` 未隨版本更新的問題。該常數在載入時會覆蓋 `index.html` 的顯示文字，導致畫面顯示的版本與實際部署不符。
- 新增自動更新檢查：以不走快取的方式比對伺服器版本標記，發現新版即重新載入；上課中與背景分頁不會觸發，並防止版本標記不同步時陷入重整迴圈。

- 抽出共用的 `TURN_CONTRACT` 放進 `buildSystemInstruction`，Gemini 與 GPT 現在共用同一份最高優先規則。先前這段只掛在 GPT 前面，Gemini 拿不到「示範後立即停止」與「同一句型家族不得連續操練」。
- `sentenceFamily` 先還原縮寫。`I'm happy` 與 `I am happy` 舊版會被判成不同家族，導致重複計數永遠到不了門檻、變異限制器實際上從未生效。
- 示範句擷取補上無引號、`Say:`、`Repeat after me:`、`跟我念` 等講法。實測六種常見講法從三種可辨識提升為六種全中；漏抓時會同時失去重複計數與 `practice` 學習紀錄。
- 早退復原指令改為全正向敘述。舊版以否定句列出禁語，等於把該句直接餵給模型，反而容易被照著講出來。

### v3.10 以前

- 修正 Gemini 將 `It's good to see you.` 的 `see you` 誤判成下課。
- Gemini 真正提早說再見時會清除已排隊的聲音，恢復課程時不再說「我們還沒下課喔」，而是自然換到另一個小挑戰。
- 修正正式 Goodbye 後又繼續問問題的流程。
- 修正 GPT 按說話鈕無法中斷，以及取消回應後太早建立新回應的競態問題。
- GPT 已接上圖片、Top 5 題目及單字記錄工具。
- Gemini 與 GPT 已共用回合完成、練習等待、階段切換及下課狀態。
- 修正 GPT 逐字稿偶爾消失。
- 修正 GPT 音訊無聲及手機從聽筒播放的問題，目前使用 WebRTC 直接播放備援。
- 修正圖片文字先切換、圖片仍停留上一張的同步問題。
- 修正 Day 5 完成後沒有切換到下一單元。
- 修正連線尚未完成便切入學生畫面的問題。

## 4. 目前架構

| 檔案 | 用途 |
|---|---|
| `index.html` | 首頁、設定頁、學生畫面與 PWA 入口 |
| `app.js` | 主程式、課程組裝、模型連線、提示詞及 UI 協調 |
| `openai-realtime.js` | GPT Realtime WebRTC、音訊、工具呼叫及中斷控制 |
| `live-session.js` | Gemini 連線生命週期、斷線重連及 session resumption |
| `student-view.js` | 學生畫面、逐字稿、圖片與新聞題目同步 |
| `practice-observer.js` | 複誦邊界、句子建議擷取及句型家族判斷 |
| `lesson-ending.js` | Goodbye 偵測、提早結束防護及正式下課判斷 |
| `stage-transition.js` | 階段切換門檻，避免回饋尚未完成就硬切 |
| `course-progression.js` | 五日課程、前後單元推進與跨單元複習來源 |
| `lesson-plan.js` | 課前組裝今日項目清單（辨識階梯、句型代換、對答），含提示階梯與執行器 |
| `images/` | 527 張單字卡與句型情境圖（512px WebP，共 18 MB），由 build-images.py 產生 |
| `build-images.py` | 「Gogo English」專案的插畫母版 → `images/*.webp` |
| `learning-records.js` | 每位學員的句子練習記錄與合併 |
| `session-diagnostics.js` | 課堂診斷事件及 JSON 匯出 |
| `sync.gs` | Apps Script 後端、同步、臨時憑證及新聞來源 |
| `units.json` | 36 個 Gogo English 單元資料（Book 1／2／3，含 Review），由產生器輸出，不要手改 |
| `units-overlay.json` | 人工整理、gogo 教材資料沒有的部分：模板化句型、英文 theme、單字例句與詞性 |
| `build-units-from-gogo.py` | `gogo{1,2,3}.json` + `units-overlay.json` → `units.json` |
| `tests/` | 瀏覽器 smoke／回歸測試 |

## 5. 已知限制與待觀察項目

- Gemini 與 GPT 都是生成式模型，即使提示詞與前端防護已加強，仍需透過實際課堂持續觀察是否偶爾偏離流程。
- Gemini 的聲音偶爾可能在句子之間改變；這主要發生在模型端，目前前端只能降低重連與上下文中斷，無法完全鎖定音色。
- 計畫模式的教材圖片走本地圖庫（`images/`），沒有延遲問題。即時生圖（Pollinations）只剩舊流程與時事模式在用，那邊仍可能延遲。
- Google Apps Script 偶爾可能出現同步超過 15 秒；本機資料仍會保留，之後可再次同步。
- 手機瀏覽器的擴音路徑受 Android、Chrome、藍牙及裝置麥克風配置影響，不同手機仍需實機測試。
- GPT Realtime 的費用高於 Gemini；建議日常使用 `gpt-realtime-2.1-mini`，品質模式只在需要時開啟。
- 新聞 RSS 標題可能重複、截斷或暫時取不到；需要繼續觀察 Top 5 的品質與兒童安全篩選。
- 目前自動測試能驗證事件與狀態流程，但無法完全取代真實語音、手機喇叭及長時間模型對話測試。
- `units-overlay.json` 裡句型的 `chinese` 寫的是「用途說明」而不是中文句子（例如
  「用於詢問並表達喜愛的食物或事物。」）。`lesson-plan.js` 會把它當成要唸給學員聽的中文提示，
  於是產生「別人問你『用於詢問並表達喜愛的食物或事物。』的時候，你要怎麼回答？」這種聽不懂的句子。
  這是舊資料就有的問題，不是換教材造成的；`gogo{1,2,3}.json` 的 `target_sentences` 有逐句中文，
  可以拿來補。
- 200 個正課單字裡有 78 個沒有例句（多半是第 2 冊新加進來的字）。例句是選填欄位，
  模型仍會拿到中文意思與單元句型，但有例句時的示範會更穩。
- `phonics`／`grammar`／`expressions` 已經在資料裡，但 `lesson-plan.js` 還沒有對應的項目型別，
  等於目前上課用不到。

## 6. 建議的下一步

教學需求的完整規格與交接說明見 [`HANDOFF.md`](HANDOFF.md)。

1. **實機驗證 v3.19 的新模板**。計畫模式現在預設開啟，連線後確認系統紀錄顯示
   `🗒️ 計畫驅動模式：…共 N 個項目`，再開始上課。重點觀察：提示階梯是否照層級揭露、
   模型會不會把還沒揭露的內容先講出來、句型代換的中文提示是否自然。
2. 學生畫面的圖片應該瞬間出現（本地檔）；若仍出現「產生圖片中」表示走錯路徑。
3. 課後匯出診斷 JSON，重點看 `plan_item_sent`、`plan_attempt`、`plan_fallback_advance` 與 `item_result_summary.adherence`，用以判斷模型是否穩定回報。
4. 觀察 v3.17 是否已消除「教到 goodbye 就被判定下課」的迴圈。
5. 依遵從率決定階段 4：開場三種回應處理、弱點累積與運用、項目層級結果寫入試算表。
6. 針對圖片延遲、Apps Script 同步逾時與手機音訊路徑，持續累積不同手機的診斷資料。

## 7. 部署與測試

- 前端部署：GitHub Pages，推送到 `main` 後自動更新。
- Apps Script 若修改 `sync.gs`：必須到「部署 → 管理部署作業 → 編輯 → 新版本 → 部署」。
- 跨手機同步設定請見 [`SETUP-SYNC.md`](SETUP-SYNC.md)。
- GPT 設定請見 [`SETUP-GPT.md`](SETUP-GPT.md)。
- 最新一次完整瀏覽器回歸測試：11 組全數通過（v3.18，2026-08-24）。
- 更新教材：先改「Gogo English」專案的 `教材資料/gogo{1,2,3}.json`，再回本專案執行
  `python build-units-from-gogo.py`。若兩個專案不在同一層，用 `--gogo <路徑>` 指定。

## 8. Git 狀態基準

最新功能提交為：

- `7e6dd94` — 對齊兩個模型的提示詞，修復重複練習偵測與早退復原
- `e6bcb23` — Prevent repetitive drills and false farewells
- `253999e` — Align GPT realtime lesson behavior
- `88147f3` — Advance completed learners to the next course unit
- `3d9a6b8` — Delay student view until ready and stabilize lesson ending

後續開發完成後，請同步更新本文件最上方的日期、版本、已知限制與下一步。
