# 跨手機同步設定（約 10 分鐘，只需做一次）

做完之後，你的手機、老婆的手機、平板都會共用**同一份學習紀錄**。
**其他手機不需要登入你的 Google 帳號**——程式是以「你」的身分執行的。

---

## 第 1 步：建立試算表

1. 到 https://sheets.google.com ，建立一個**空白試算表**
2. 檔名取叫 `AI Tutor 學習紀錄`（名字隨意）

> 工作表不用自己開，程式第一次執行時會自動建立 `vocab` 和 `state` 兩個分頁。

---

## 第 2 步：貼上後端程式

1. 在這個試算表上方選 **擴充功能 → Apps Script**
2. 把編輯器裡原有的內容**全部刪掉**
3. 打開專案資料夾裡的 **`sync.gs`**，把整份內容複製、貼進去
4. 找到第 15 行左右這一行，把它改成**你自己想的一串密語**（等一下手機上要填一樣的）：

   ```javascript
   const SECRET = '請改成你自己的通關密語';
   ```

   例如改成 `const SECRET = 'gogo-rex-jessie-sandy-2026';`

5. 按上方的 💾 **儲存**

---

## 第 3 步：部署成網頁應用程式

1. 右上角按 **部署 → 新增部署作業**
2. 左邊齒輪 ⚙️ 選 **網頁應用程式**
3. 設定成這兩項（**很重要，設錯就不會動**）：

   | 項目 | 要選什麼 | 為什麼 |
   |------|---------|--------|
   | 執行身分 | **我（你的信箱）** | 這樣其他手機才不用登入 |
   | 誰可以存取 | **任何人** | 手機端才連得上 |

4. 按 **部署**
5. 第一次會跳出授權：**授權存取 → 選你的帳號 → 進階 → 前往「未經驗證的應用程式」→ 允許**
   （因為這是你自己寫的程式，Google 尚未驗證，屬正常流程）
6. 複製最後給你的**網頁應用程式網址**，長得像：

   ```
   https://script.google.com/macros/s/AKfycb.....................pw/exec
   ```

> ✅ **確認成功**：把這個網址直接貼到瀏覽器打開，應該看到
> `{"ok":true,"msg":"AI Tutor sync is running."}`

---

## 第 4 步：在每支手機上填入

在 App 裡按 **⚙️ 設定 → ☁️ 跨手機同步**：

1. **Apps Script 網址**：貼上第 3 步複製的網址
2. **通關密語**：填第 2 步你自己設定的那串字
3. 按 **🔄 立即同步**，看到綠色的「✅ 已同步」就成功了

每支手機各做一次，之後就會自動同步。

---

## 平常怎麼運作

- **開啟 App 時**：自動取回最新紀錄
- **上課學到新單字、換單元、進度前進**：自動在背景上傳（約 3 秒後）
- **想手動確認**：隨時按「🔄 立即同步」

兩支手機同時上傳也沒問題，後端會排隊處理並把單字**取聯集**合併，不會互相覆蓋。

---

## 額外好處：家長報告

打開那個試算表的 **`vocab` 分頁**，你會看到每個孩子的完整學習歷程：

| person | word | meaning | example | firstDate | lastDate | count | unit |
|--------|------|---------|---------|-----------|----------|-------|------|
| Rex | science | 科學 | I like science. | 2026-07-26 | 2026-07-28 | 3 | Book 3 Unit 5 |

可以直接篩選、做圖表，或印出來看孩子這個月學了什麼。

---

## 常見問題

**Q：老婆的手機需要登入我的 Google 帳號嗎？**
不用。手機只是把資料送到那個網址，帳號驗證是 Google 那邊用**你的身分**完成的。

**Q：那別人知道網址不就能亂寫？**
所以才有「通關密語」——密語不對的請求會直接被拒絕。網址也不會寫進公開的程式碼裡，只存在你自己的手機。

**Q：同步失敗怎麼辦？**
先確認：① 網址結尾是 `/exec`、② 密語兩邊一致、③ 部署設定是「以我的身分執行 + 任何人可存取」。
改過 `sync.gs` 之後要重新 **部署 → 管理部署作業 → 編輯 ✏️ → 版本選「新版本」→ 部署**，否則改動不會生效。

**Q：沒設定同步會怎樣？**
完全不影響，紀錄照樣存在該支手機裡，只是不會跨裝置共用。
